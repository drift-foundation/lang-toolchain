# TODO

[String]
- Add parameter lowering in the LLVM backend (support non-void params, including `%DriftString`).
- Wire string builtins/ops: `string_len`, `string_eq`, `string_concat` (and associated codegen), plus tests covering them.
