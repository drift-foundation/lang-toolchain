import std.io as io;

pub fn main() nothrow -> Int {
	var buf = io.buffer(1);
	val b = cast<Byte>(65);
	io.buffer_write(&mut buf, 0, b);
	val r = io.buffer_read(&mut buf, 0);
	val v = cast<Int>(r);
	return (v == 65) ? 0 : 1;
}
