import std.core as core;

struct S {
	arr: Array<Int>,
}

pub fn main() nothrow -> Int {
	try {
		val r: core.Result<S, Int> = core.Result::Ok(S(arr = [1]));
		val s = r.or_throw();
		return s.arr.len;
	} catch std.err:ResultError(_) {
		return 0;
	} catch {
		return 0;
	}
}
