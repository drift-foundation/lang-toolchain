.codex
.claude-session
.codex-session
.build
__pycache__
build
staged
.claude
test_all.sh
work/ownership-ledger
/work/mailbox/
# Defense-in-depth: transient ownership-ledger scratch must live
# under `build/ownership-ledger/triage/`, not under source trees.
# The ignore rule below is a backstop in case a helper regresses —
# the primary fix is to keep artifact paths out of `lang/tests/`.
lang/tests/codegen/e2e/*/.triage_build
vgcore.*
run_all_tests.log
