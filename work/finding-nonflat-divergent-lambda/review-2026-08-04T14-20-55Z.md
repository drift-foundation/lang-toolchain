# Promotion execution handoff — reproduce reviewed environment

State: promotion execution requested; prior promotion attempt failed closed before compilation or baseline mutation because the reviewer process had a different fingerprinted `PATH`.

## Observed failure and root cause

The authorized reviewer invocation of `just ownership-corpus-promote` exited 2 with:

`handoff ... ownership-corpus-projection.json was produced under a different toolchain than the current tree; it is stale`

The reviewed baseline remains untouched.

An independent component-by-component fingerprint comparison found exactly one changed component: `env.PATH`.

- Candidate/check environment begins with the normal `/home/sl/.local/bin` and NVM/system paths.
- The reviewer execution environment prepends Codex vendor and temporary wrapper directories.
- `compile_source`, stdlib, audit tool, runtime, native libraries, tool binaries, argv template, contract schema, and every other fingerprinted environment entry match.

The guard is behaving correctly. Do not regenerate the candidate or promote a baseline under the reviewer-specific injected path merely to bypass it.

## Requested action

Slawomir's conditional authorization is satisfied by the positive reviewer and implementer audits. In the same unchanged shell environment that successfully ran `just ownership-corpus-check` and produced toolchain composite `59a61c8e6333ddbe61b365e4631666cfa1d257ed46b05c3e8b5c64fc338a5e33`, run:

```text
just ownership-corpus-promote
```

Do not override `PATH`, regenerate the projection, or edit baseline files manually.

If promotion succeeds, independently inspect the tracked reviewed-baseline diff and confirm that aggregate/projections remain byte-identical while manifest/fingerprint/metadata reflect the reviewed identity refresh. Record the command result and exact diff scope in `PROGRESS.md`, consume this review token, and create a work-level `IMPL-PENDING-<timestamp>` pointing to `finding-nonflat-divergent-lambda/PROGRESS.md`.

If it fails, stop without a stronger retry, preserve the output/retained actual, record the evidence, and return the same implementation handoff for review.
