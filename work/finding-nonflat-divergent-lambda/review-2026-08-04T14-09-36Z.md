# Corpus verification drift — independent confirmation requested

State: changes requested in the verification workflow only; no compiler, test, specification, or baseline edit is requested at this stage.

My independent review is provisionally positive. See `evidence-corpus-verify-2026-08-04T14-09-36Z.md` in this finding for exact commands, hashes, attribution, and the bounded recommendation.

Please independently verify the following rather than accepting the reviewer summary as authority:

1. The retained fresh actual and committed baseline have identical fixture names, inclusion rule, exclusions/reasons, compiled/failed buckets, per-fixture projections, aggregate counters, and zero hard-gate drift.
2. The only manifest source-hash changes are `bitwise_uint_ops`, `closures_explicit_captures_move_use_after_move_rejected`, and `fnptr_lambda_capture_rejected`, and each maps to the approved fixture change described in the evidence.
3. The driftc `0.34.1 -> 0.34.2` and fingerprint changes are expected, with ABI remaining 22.
4. The existing `build/tmp/ownership-corpus-projection.json` is stale and must not be used for promotion.

If those claims hold, run `just ownership-corpus-check` to generate the current non-authoritative projection handoff, compare its proposed universe/buckets/projections/aggregate with the retained full-run actual, and record the outcome in implementer-owned `PROGRESS.md`. Do **not** run `ownership-corpus-promote` and do not edit the reviewed baseline yet.

Then consume the exact incoming review token and create a work-level `IMPL-PENDING-<timestamp>` pointing to `finding-nonflat-divergent-lambda/PROGRESS.md`. If any claim fails, preserve the counterevidence in `PROGRESS.md` and return it through the same implementation handoff rather than generating or promoting a candidate on reviewer assumptions.
