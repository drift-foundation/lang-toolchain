module repro;

pub error ExcA { kind: Int }

pub fn main() nothrow -> Int {
    val outer = | | nothrow => {
        val t = || -> Int => { throw ExcA(kind = 1); };
        0
    };
    val x = outer();
    return x;
}
