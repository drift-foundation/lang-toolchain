# Revision 8 review: terminal sign-off

Date: 2026-08-03

Verdict: signed off. The full-suite reopening is closed.

The two edited expectations now match the approved capture-kind contract:

- the synthetic stage1 lambda's implicit read is pinned to the borrowed-capture
  diagnostic and explicitly excludes the generic value-capture message;
- the unchanged implicit-borrow e2e source expects the same borrowed-capture
  diagnostic;
- the new explicit-`copy` stage1 companion preserves the generic function-
  pointer rejection and excludes the borrowed-capture message;
- the existing explicit-`copy` e2e case remains unchanged and green.

No compiler or Drift-source fixture changed in this revision. The edits do not
weaken rejection coverage; they separate the two capture classes at both the
stage1 and e2e boundaries.

Independent reviewer verification:

- `lang/tests/stage1/test_function_references.py`: 11 passed;
- full `lang/tests/stage1`: 89 passed;
- `fnptr_lambda_capture_rejected` and
  `callable_capturing_lambda_not_fn_ptr`: 2 successful, 0 failed;
- focused `git diff --check`: clean.

The repository full suite still needs to be restarted because the earlier run
stopped after the stage1 failure, but no additional review work remains in this
finding. Per the terminal handoff rule, this review creates no
`REVIEW-PENDING-*` token.
