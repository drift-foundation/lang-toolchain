# Spec-authority hold — 2026-08-03T16:54:12Z

## Status

**Implementation hold; human ruling required.** This addendum supersedes the
Revision 6 review's direction to reconcile language-spec text. Slawomir added a
strict repository rule on 2026-08-03: no language-spec or spec-change-request
document may be edited, reinterpreted, or used to introduce a new language
contract without his explicit, case-specific approval. The committed language
specification remains authoritative until that approval exists.

Do not make further spec edits or continue semantic implementation changes for
the bare-stored-closure contract in response to the preceding review. This is
not a request to choose either semantic outcome; it is a stop-and-confirm gate.

## Active conflict

The committed `HEAD` specification says:

- `doc/design/drift-lang-spec.md:4507`: capturing lambda literals produce
  compiler-synthesized closure values, with `copy`/`move` captures escaping;
- `doc/design/drift-lang-spec.md:4620`: closures with
  `copy`/`move`/`share` captures may escape;
- `doc/design/spec-change-requests/drift-closure-explicit-captures.md:137-169`
  describes closure-owned loans, moving a closure value, and producing the
  closure value during lowering.

The current implementation instead rejects a bare stored value-capture lambda
at `lang/driftc/type_checker.py:13735-13764` because no anonymous closure-value
type is assumed to exist. The worktree also currently contains unapproved edits
to `doc/design/drift-lang-spec.md:4507` and `:4620-4628` that change the spec to
match that implementation. Those edits may have been made before the new rule
was communicated; regardless of timing, they are not an approved contract and
cannot be used as review authority.

The existing e2e
`lang/tests/codegen/e2e/closures_explicit_captures_move_use_after_move_rejected/`
is additional executable evidence for the committed contract: it stores a
bare `captures(move x)` closure and expects construction to consume `x`.

## Decision required from Slawomir

Please present, without implementing either choice further:

1. **Keep the committed contract.** Bare stored value-capture closures remain
   valid. The reject-at-binding path and its replacement tests must not land;
   the compiler needs a real closure-value representation/lowering path that
   satisfies the existing spec and ownership regression.
2. **Approve a language-contract change.** Bare stored capturing lambdas become
   invalid in v1 and escape is contextual through callback/Fn-bounded
   representations only. If Slawomir explicitly approves this change, the
   compiler/test/spec migration can proceed under the approved wording and
   compatibility consequences.

Until Slawomir selects and approves one of these semantic directions, this
finding cannot be signed off. `PROGRESS.md` should record the hold, but the
reviewer will not edit that implementer-owned file.
