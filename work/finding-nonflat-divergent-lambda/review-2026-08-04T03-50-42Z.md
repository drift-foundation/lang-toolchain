# Review — full-suite blocker children

Timestamp: 2026-08-04T03:50:42Z

Outcome: signed off. No blocking compiler finding remains in the two children
opened from the memcheck codegen-e2e failure.

## Child A — poisoned catch binder

The root fix is sound on static review: the phase-1 HVar resolver no longer
uses function-wide binding-name history after active lexical lookup fails.
The unresolved expression now produces `E-UNKNOWN-NAME`, becomes `Unknown`,
and the shared return authority suppresses the derivative mismatch. No global
diagnostic-count suppression or catch/`Error` special case was added.

Reviewer verification:

```text
test_out_of_scope_name_resolution.py + test_catch_binder_sibling_name_reuse.py: 7 passed
catch_binder_scope_leak: ok
```

## Child B — `Uint` bitwise fixture

The Slawomir-approved migration is exactly the reviewed one-line explicit
boundary cast. `main` remains `nothrow -> Int`; no implicit `Uint -> Int`
return conversion was added.

Reviewer verification:

```text
bitwise_uint_ops: ok
test_lambda_return_inference_boundary.py: 15 passed
```

`git diff --check` is clean.

## Non-blocking observations

The new driver test contains an ephemeral finding-path reference, its
same-typed shadowing shape does not distinguish the two bindings, and parent
progress calls one compile/link positive “compile+run.” Slawomir explicitly
directed that non-critical review gaps be skipped to prioritize the full
`run-all-tests.sh` gate. These do not undermine the compiler fix or the two
original full-suite regressions, so no further review handoff is opened.

This review consumes `IMPL-PENDING-2026-08-04T03-43-48Z`. Per the terminal
handshake rule, it creates no `REVIEW-PENDING-*` token.

