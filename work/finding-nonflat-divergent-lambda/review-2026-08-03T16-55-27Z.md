# Revision 7 static review — 2026-08-03T16:55:27Z

## Verdict

**Signed off.** No remaining findings in `finding-nonflat-divergent-lambda`.

Static review only; I did not rerun K's reported 66/66 focused contract tests,
the migrated e2e, the broader 162-test battery, the full suite, or corpus gates.

## Spec-authority resolution

Slawomir explicitly approved the current language-spec edits on 2026-08-03
after the spec-authority hold was published. That human approval resolves
`review-2026-08-03T16-54-12Z.md` and authorizes the contextual-only v1 closure
contract represented by the current diff:

- a capturing lambda may be immediately invoked or converted through a
  supported callback/Fn-bounded representation;
- no standalone anonymous capturing-closure value exists in v1;
- a bare stored capturing lambda is rejected at its binding;
- value-capture ownership effects occur when the supported representation is
  constructed.

The hold token crossed K's Revision 7 handoff in flight and was resolved
directly by Slawomir before K could consume it. It is withdrawn with this
sign-off so no terminal `REVIEW-PENDING-*` token remains.

## Revision 7 closure

- `doc/design/drift-lang-spec.md` now states the approved contract consistently
  in both the callable taxonomy and escape section.
- The explicit-capture spec-change request clearly marks its raw standalone
  closure-value language superseded and scopes creation/move semantics to the
  supported representation.
- `closures_explicit_captures_move_use_after_move_rejected` now constructs a
  `core.Callback0<Int>` with `captures(move x)` and then reads `x`. Its existing
  borrow-check expectation therefore continues to pin construction-time move
  semantics on a legal representation, even though the callback is never
  invoked.
- A fresh static inventory found only the intentional bare-storage negatives;
  the annotated function-pointer rejection and contextual callback-field form
  are separate, valid boundaries.

## Parent finding closure

The prior rounds' implementation findings remain closed:

- terminal/no-fallthrough and can-throw analysis share `hir_flow.py`, including
  CFG reachability, dead catch arms, lazy expression evaluation, nested-lambda
  boundaries, and conservative unknown-node handling;
- non-flat divergent lambda bodies lower through the full pipeline rather than
  being checker-rejected to mask lowering;
- the captureless/hidden/thunk/instantiation scheduler reaches the reviewed
  joint fixed point, with thunk completion in the quiescence predicate;
- captureless uninvoked stored lambdas receive a concrete fn-pointer lowering
  form or a clean unresolved-inference diagnostic;
- bare stored capturing lambdas follow the now-approved binding-time rejection,
  and raw `HLambda` at HLet lowering is only a defensive contract assertion;
- parent/child progress records and focused test counts agree with the current
  files.

The separately queued lambda-return reconciliation, lambda-tail coercion, and
CallInfo inference findings remain independent and are not part of this
sign-off.
