# Full-suite reopening: stale stage1 capture diagnostic contract

Date: 2026-08-03

Verdict: changes requested. Reopen this finding; do not create a separate
finding for this failure.

## P1 — the approved capture-kind contract was not migrated through stage1

The full suite fails at
`lang/tests/stage1/test_function_references.py::test_capturing_lambda_rejected_for_fn_pointer`.
The synthetic lambda implicitly reads outer `y`, so capture discovery produces
a borrowed capture. Revision 4 normalized implicit capture kinds specifically
so this route emits:

```text
closures with borrowed captures are non-escaping in v0; only immediate invocation or proven non-retaining params are supported
```

The test still requires the superseded generic value-capture diagnostic:

```text
capturing lambdas cannot be coerced to function pointers
```

This is part of the same finding: it is an in-tree consumer of the capture-kind
classification and diagnostic contract changed here. The focused gates missed
it, so the previous sign-off was premature once the full-suite result became
available.

Update the stage1 pin to assert the borrowed-capture diagnostic for this
implicit-borrow shape. Preserve separate generic function-pointer rejection
coverage for a value capture (`copy`/`move`) if existing stage1 coverage does
not already make that distinction. Do not change the compiler back merely to
satisfy this stale assertion: driver/e2e coverage already pins the normalized
borrowed-capture behavior approved in this finding.

This requires editing an existing test. Under the repository rule, wait for
Slawomir's explicit approval of that test edit before implementing it.

Before handing back:

- run the exact failing stage1 test;
- run the whole `lang/tests/stage1/test_function_references.py` file;
- scan stage1 tests for other assertions that conflate borrowed and value
  captures;
- report whether the e2e generic-message pins use value captures and therefore
  remain valid;
- re-run the `stage1` suite job at minimum.

No language-spec change is needed. This review does not identify a new compiler
semantic defect; it identifies an incomplete test migration in the already
approved contract change.
