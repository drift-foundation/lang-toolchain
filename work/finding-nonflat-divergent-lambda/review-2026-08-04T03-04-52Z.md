# Review — full-suite reopen after shared return authority

Timestamp: 2026-08-04T03:04:52Z

Outcome: changes requested. The current parent finding is not terminally
signed off because the memcheck `lang-codegen-test` gate found two failures.

## P1 — out-of-scope catch binder is resolved from function-wide history

`catch_binder_scope_leak` expects `unknown name 'e'` after the catch scope, but
phase 1 now emits `return type 'Error' does not match declared type 'Int'`.

The concrete source is the HVar fallback around `type_checker.py:7334`: after
active lexical lookup fails, it scans all function-wide `binding_names` and
rebinds the unresolved HVar to the popped catch binder. The new return check
then exposes that stale `Error` type and prevents the later checker from
supplying the primary unknown-name diagnostic.

Fix lexical resolution rather than suppressing the return mismatch. Audit why
the fallback exists before removing it; any legitimate producer that depends
on it should be repaired to carry binding identity or use active scope.

Detailed evidence and proposed acceptance coverage are in:

`findings/finding-return-poisoned-catch-binder/`

## P1 gate — `bitwise_uint_ops` appears to be a stale fixture, not a compiler coercion gap

The case declares `main -> Int` but returns a non-literal `Uint`. The new clean
diagnostic is `return type 'Uint' does not match declared type 'Int'`.

The checked-in spec requires `main -> Int`, distinguishes `Int`/`Uint`, and
provides explicit numeric casts. I found no contract authorizing implicit
non-literal `Uint -> Int` returns. The likely correct migration is
`return cast<Int>(x);`, preserving exit 254 and all bitwise coverage.

Do not weaken the return authority for this fixture. Because this is an edit
to an existing test, record the proposal and use the `APPROVAL-PENDING-*`
protocol; no edit is authorized by this review alone.

Detailed evidence is in:

`findings/finding-return-uint-main-compatibility/`

## Verification reproduced by reviewer

```text
[codegen e2e] bitwise_uint_ops: FAIL (unexpected checker diagnostics: return type 'Uint' does not match declared type 'Int')
[codegen e2e] catch_binder_scope_leak: FAIL (missing expected diagnostic: return type 'Error' does not match declared type 'Int')
Tests: 2 (0 successful, 0 skipped, 2 failed)
```

The complete `doc/refactor_triggers.md` registry was scanned; neither child
matches a live trigger.

## Handoff protocol

- Create implementer-owned `PROGRESS.md` files in both child roots when picked
  up, and summarize both child statuses in the parent `PROGRESS.md`.
- The older `REVIEW-PENDING-2026-08-03T23-05-59Z` is still present even though
  parent progress was updated after it landed. Consume that stale exact token
  as protocol cleanup.
- Consume the new token only after addressing this review in `PROGRESS.md`.
- If blocked on the existing `bitwise_uint_ops` edit, publish the exact
  `APPROVAL-PENDING-*` gate; continue the independent catch-binder fix while
  awaiting Slawomir's ruling.
- When both children are resolved and focused gates are green, publish one new
  timestamped parent `IMPL-PENDING-*` handoff.

