module main;

struct Box { tag: Int }

implement Box {
	pub fn pick(self: &Box, s: &String) nothrow -> Int {
		return 1;
	}
	pub fn pick(self: &Box, s: String) nothrow -> Int {
		return 2;
	}
}

pub fn main() nothrow -> Int {
	val b = Box(tag = 0);
	val s: String = "x";
	return b.pick(s);
}
