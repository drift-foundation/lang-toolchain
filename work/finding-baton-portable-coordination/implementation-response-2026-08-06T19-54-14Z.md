# Baton message

Timestamp: 2026-08-06T19-54-14Z
From role: implementer
Actor: k
To role: reviewer
Kind: implementation_handoff
Thread: baton_portable_coordination_v6
Move round-5 revision — cycle complete, hashes computed after the last write and re-verified before composing. FROZEN boundary: tools/baton/baton_v6.py 2353 lines, sha256 8c02ed98c9134f6545309c7025250a12c11f1e56d1a6ed54fb6d5e8bb800a27b; tools/baton/test_baton_v6.py 2159 lines, sha256 ad8aca1fe1b0bc8283347407fe81b8df369a558f94309de0b81efa63e008e8d9. Full suite 167/167; both files untouched until your reply.

Your post-bind clone repro is closed and pinned red-first: move_activate now revalidates physical residence at the bound route (parent inode + basename) before committing, so a destination-role clone or rename refuses with EXIT_DAMAGE anywhere but the exact bound path — the pin attempts the rogue activation BEFORE the real one and then proves exactly one path accepts writes; bound-clone bind-retry and activated-clone activate-retry refusals are separate pins. Routing history: ceremonies gains an immutable peer column recorded by bind, activate, and decommission; every committed fast path now validates route as well as token — your wrong-moved_to retry repro rejects with "differs from the committed route" while the exact retry stays already_committed, the bind/activate fast paths revalidate residence against the ceremony's recorded route, and move_copy post-activation discovery validates the retained ceremony peer rather than token+UUID alone. The committed-boundary crash matrix is in: post-COMMIT/pre-return fault hooks in maintenance_enter(move), bind, activate, and decommission, each killed in a subprocess and recovered by a fresh process — the enter crash is recovered by discovering the committed token/route through the new readonly move_status_inspect and completing the same move end-to-end, the other three re-invoke to already_committed, and the matrix asserts exactly one active authority at the end. The DB copy is now streaming: bounded chunks pread from the held fd into the scratch with inline hashing, fsync, no-clobber publish; resume stream-hashes an existing regular artifact opened nonblocking/no-follow (a FIFO can neither hang nor impersonate), and premature EOF, zero-byte writes, source-changed-mid-stream, and mismatching artifacts all fail closed — pinned with tiny-chunk and hostile-artifact tests rather than large fixtures. Stage-discovery recovery classification is narrowed to the two expected absence shapes; any other error from an existing pair propagates with its own reason. PROGRESS.md updated; wait/CLI remains paused.
