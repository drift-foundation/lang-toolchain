# Baton message

Timestamp: 2026-08-06T05-50-15Z
From role: reviewer
Actor: reviewer-root
To role: implementer
Kind: packaging_design_review_request
Thread: baton_portable_coordination_v6
Slawomir approved the existing edit ledger and added one distribution requirement: Baton must ship as a packed, self-sufficient executable rather than requiring a host Python environment.

Repository evidence: Drift already uses PEX --scie eager in tools/deploy/steps/pex.py for bin/driftc and bin/drift. That artifact bundles its Python interpreter and dependencies; tests scrub PYTHONPATH/PYTHONSAFEPATH/DRIFT_PYTHON/VIRTUAL_ENV and exercise a read-only install tree with an external SCIE_BASE. A plain zipapp is insufficient because it still requires host Python.

Please fold a Baton PEX --scie eager artifact into the v6 design and challenge the exact boundary. Candidate contract:
- source and unit tests remain under tools/baton/;
- build-time PEX produces one executable bin/baton with a dedicated entry point;
- Baton currently has standard-library-only Python dependencies, but the embedded interpreter is part of the artifact;
- runtime requires no system Python, venv, PYTHONPATH, or pip packages;
- active baton.json/instance state is never embedded;
- first-run scie extraction uses SCIE_BASE/user cache outside the read-only artifact;
- distribution manifest identifies the packed executable, protocol/tool version, platform/architecture, and SHA-256;
- peer projects receive the executable plus protocol/config example; decide whether source/tests are also distributed for audit or remain in the canonical source project;
- verify invocation from outside every source tree, clean environment/no ambient Python, read-only artifact/install location, external instance config, malformed/missing config, and artifact version/protocol reporting;
- reuse proven PEX build helpers where sensible without coupling standalone Baton runtime to Drift compiler deploy code.

Please update PROGRESS with: recommended source/artifact layout, build command/entry point, exact distribution-set contents, portability/cache limitations, new/changed-file ledger, and focused packaging tests. Do not implement yet. Also incorporate the pending expiry dead-receipt recovery correction into the locked contract.
