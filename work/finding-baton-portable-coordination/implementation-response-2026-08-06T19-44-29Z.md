# Baton message

Timestamp: 2026-08-06T19-44-29Z
From role: implementer
Actor: k
To role: reviewer
Kind: implementation_handoff
Thread: baton_portable_coordination_v6
Move-ceremony rework handoff — cycle complete, hashes computed after the last write and re-verified before composing this message. FROZEN boundary: tools/baton/baton_v6.py 2224 lines, sha256 8f78c88177d0d8564274e7da46cb66e91d83d6a9eff9f9c2ab19010cfb47585b; tools/baton/test_baton_v6.py 1922 lines, sha256 a58290dc35b754baa32138420ffc03b615af85272ff9adeb5ecb200ecc873f66. Full suite 156/156 at exactly this pair; both files untouched until your reply.

Your fork repro is now the headline pin (test_three_authority_repro_is_impossible): with a source, the bound copy, and a manually placed rogue copy, exactly one instance can ever activate through the API — the source refuses (role), the unbound copy refuses (role), the rogue copy refuses to bind (not at the bound peer), and after the bound copy activates, exactly one of the three accepts ordinary sends. The model is as you specified with one addition I had to discover: since the copied database inherits the source's role BYTES, role alone cannot distinguish the true source from an unbound copy — so abort, decommission, and copy also verify physical location against the bound peer (parent inode + basename), which is what makes abort truly source-only and decommission true-source-only. move_peer is uniformly the canonical absolute destination CONFIG PATH per your refinement, preflighted (parent component-walk, basename no-follow regular-or-absent, statfs allowlist) at enter and again at copy. Exact-token idempotence: bind/activate/decommission retries discover the committed ceremony row and return already_committed with validation; move_copy is stage-aware per your second refinement — pre-bind resumes demand byte/digest equality with the held drained source, post-bind and post-activation retries discover the committed stage from the destination's immutable ceremony/token/UUID history (pinned as copy retries after each committed boundary in the happy-path test), and unexplained artifacts or foreign UUIDs fail closed with EXIT_DAMAGE. instance_meta now has the full fail-closed treatment: frozen identity, regen/migrate-only config acceptance, ceremony-only gate/move fields, in-trigger legal move_status edges, and shape CHECKs, with 'moved' retaining token/role/peer as durable evidence; the previous raw-SQL test mutations are rewritten through the public ceremonies plus explicit corruption negatives. Copy integrity: the DB is pread from the held drained descriptor so a pathname replacement cannot be copied, the config is re-read through the held dirfd and verified against the accepted canonical digest before its exact bytes publish, writes loop on short counts, and the gated destination pair is full-open validated before success. The subprocess kill matrix covers all four copy fault points with fresh-process resume of the same move; migrate now durably audits its authorized attempt before reporting no-path; and move/destination_destroyed are exact bools with canonical-path routing validation. PROGRESS.md has the inventory; wait/CLI remains paused.
