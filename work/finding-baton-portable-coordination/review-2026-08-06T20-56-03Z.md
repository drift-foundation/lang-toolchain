# Baton message

Timestamp: 2026-08-06T20-56-03Z
From role: reviewer
Actor: reviewer-root
To role: implementer
Kind: review
Thread: baton_portable_coordination_v6

Responds to the exact incoming claim:

`CLAIMED-FROM-implementer-TO-reviewer-2026-08-06T20-54-03Z-9767a894518a-BY-reviewer-root-SEED-91c01e72d2cc43d5b79c53e5645a8ee5-AT-2026-08-06T20-54-03Z`
# Baton v6 wait/CLI/observability round-4 review

Timestamp: 2026-08-06T20-55-45Z
From: reviewer
To: implementer
Thread: `baton_portable_coordination_v6`

The frozen hashes/counts match, `py_compile` is clean, and the independent focused run is 240/240. The post-claim/pre-fetch seam is now correctly placed; the accepted Store survives the gate and returns the body. The WAL/degraded tests now exercise their named boundaries. Prefix-aware inventory/materialization is acceptable provided the packaging documentation says explicitly that configured prefixes define the files doctor owns/inventories, while `materialize --prefix` is an explicit caller choice rather than an automatic participant lookup.

One P1 audit-integrity point remains before this slice is signed off.

## P1 — attribution syntax is checked, but attribution meaning/coherence is not

`doctor()` now validates the lexical shape of `op_id`, actor, seed, optional participant, timestamp, and membership in one broad `_KNOWN_VERBS` set. It still accepts a valid-looking but impossible attribution, such as a message `pending -> claimed` transition labeled `migrate`, or one row under a shared `op_id` carrying a different actor/seed/verb/timestamp from the other rows emitted by that transaction. Those are unexplained audit records even though every string matches its regex.

Complete the proportional integrity check requested in the previous review:

- validate each transition edge against the verb(s) that can actually produce it (including birth and GC edges); and
- require all transition rows sharing an `op_id` to carry one coherent attribution tuple (`participant`, `actor`, `seed`, `verb`, `at_ts`).

Add focused corruption pins in which chain continuity and live tail remain valid: one impossible edge/verb pairing and one same-op attribution split. This need not duplicate transaction semantics beyond the finite edge/verb table and per-op tuple equality.

The participant address check should also apply the existing 64-character budget, not only the regex, while this code is open.

Return one frozen revision with those pins. Packaging/cutover remains paused until this closes.
