# Baton message

Timestamp: 2026-08-06T19-30-23Z
From role: implementer
Actor: k
To role: reviewer
Kind: implementation_handoff
Thread: baton_portable_coordination_v6
Maintenance/move/migrate phase handoff — the edit/test cycle is complete and hashes were computed after the last write, then re-verified before composing this message. FROZEN boundary: tools/baton/baton_v6.py 1970 lines, sha256 d88867de5e816f4300f6862f4db489d29453422d8022be06c1128924d47000c4; tools/baton/test_baton_v6.py 1774 lines, sha256 94503e1586d2435ec7a690c5ab2c4f7c9410aa93d12af0f687290fcd080d87d6. Full focused suite 149/149 at exactly this pair; both files untouched until your reply.

What this phase adds. Schema: an immutable insert-context-guarded ceremonies audit table (maintenance_enter/exit, move_activate/decommission, abort_move, migrate) and two instance_meta CHECKs — moving/moved implies maintenance, and move_status='none' iff token NULL. A ceremony open mode that still runs the complete open validation but admits gated instances for the authorized ceremonies (ordinary writes stay gated at both open and transaction time). maintenance_enter requires the config capability plus a reason, optionally mints the move token, and refuses double entry; maintenance_exit DEFAULT-REFUSES on a moving store — completing or aborting the move are the only exits, which is the pinned guarantee that a copied same-UUID instance can never be generically un-gated into a fork. checkpoint_drain is the corrected no-transaction TRUNCATE loop requiring busy==0 AND log==checkpointed with bounded backoff, failing with the flag left set; the reader-blocks-then-converges behavior is a test. move_copy demands the move gate, drains, verifies the WAL truncated, and copies config+DB into a component-walk-validated destination via the scratch/fsync/no-clobber-hardlink/dirfsync discipline — the copy lands gated, and a populated destination refuses. move_activate and move_decommission are exact-token audited transactions: the destination clears its gate only after full open validation, the source becomes 'moved' forever with ordinary operations failing closed toward moved_to, and uuid identity is preserved end-to-end (pinned). abort_move needs the exact token, a destination-destroyed attestation, and a reason. migrate_instance is the capability-plus-maintenance-gated entry point that honestly reports there is no migration path from protocol 6. The T15 pins cover the full happy path, the zero-active crash window between copy and activation with human-only recovery in both directions, token mismatches, and the plain-vs-move gate distinction.

Remaining phases after this review: wait/eventing, CLI/projections/scan/dump/doctor/materialize, then packaging/DISTRIBUTION and the coordinated cutover. PROGRESS.md updated.
