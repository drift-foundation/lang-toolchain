# Baton message

Timestamp: 2026-08-06T20-39-24Z
From role: implementer
Actor: k
To role: reviewer
Kind: implementation_handoff
Thread: baton_portable_coordination_v6
wait/CLI/observability round-2 revision — all five groups closed; hashes captured by the verification run after the final write. FROZEN boundary: tools/baton/baton_v6.py 3128 lines, sha256 b4431c2700f699bb7819b3c20422b475ea40e0f9a9e3b161ab05ce7928cee685; tools/baton/test_baton_v6.py 2978 lines, sha256 cf47a6cefc01c9e0ec0a5c42dce69550de5756816acfec3cd21524a6f13fbfe3. Full suite 219/219; both files untouched until your reply.

Delivery: claim and wait now share one lossless shape — claim metadata plus the immutable envelope whose body is {base64, size, sha256, utf8-only-when-clean} or None, with the complete pinned attachment tuple and no fake body — pinned for text, an all-256-byte non-UTF-8 body, the empty body, attachment delivery, and the claimed-transient-readable-until-consumed contract. CLI totality: parser failures now return the documented validation code 4 with help/version at 0 and 2 reserved for floors; missing/unreadable body files, non-UTF-8 configs, bad attach syntax, and bad numerics all produce one clean diagnostic with no traceback (subprocess-style pins); send has a real argparse mutually-exclusive body/attach contract — attach alone is attachment-only, both rejects at the parser, neither defaults to stdin — and --no-body is gone; the actual CLI attachment path is pinned end-to-end. Eventing: the wait:armed seam proves publication during watch construction is caught by the requery; a WAL checkpoint/reset between waits still wakes; synthesized overflow, ignored, move-self, and unmount flags each force a counted, fully validated reopen before rearm; gating the instance WHILE a waiter is blocked stands it down with the gate code; the degraded path honors the configured interval (the hard 1s cap is removed); timeout and rescan are validated as finite reals with bool excluded, pinned at API and CLI; IN_UNMOUNT is in the armed mask. Doctor now implements the planned logical checks — ledger birth consistency for messages and claims, orphan content detection, attachment pins validated against accepted_roots including binding generation, and accepted-roots/config coherence — under explicit problems-versus-warnings semantics where ok and the CLI exit derive from problems only and recoverable residue warns, with dirfd-anchored enumeration and corruption pins; materialize requires retention='durable', refusing pending AND claimed transient messages while durable output stays byte-exact and idempotent. Dump includes op_context and labels its transitions-tail truncation alongside the total; protocol JSON encoding is explicit and fail-closed with default=str removed; notice bodies share the lossless representation. Packaging/cutover remains paused for your confirmation. PROGRESS.md updated.
