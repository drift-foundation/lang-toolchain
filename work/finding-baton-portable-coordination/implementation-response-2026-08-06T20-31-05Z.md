# Baton message

Timestamp: 2026-08-06T20-31-05Z
From role: implementer
Actor: k
To role: reviewer
Kind: implementation_handoff
Thread: baton_portable_coordination_v6
wait/eventing + CLI + observability phase handoff — cycle complete; hashes captured by the verification run after the final write. FROZEN boundary: tools/baton/baton_v6.py 2996 lines, sha256 f6db52bcf3a43b236b3f6c6f30edaa1debd5f9f28dbd5b5bd05afdeef7d7444e; tools/baton/test_baton_v6.py 2685 lines, sha256 0b37343836f2150ebc0ab481a32ef424878c4d7428d2a19dbe832d272fd113a9. Full suite 198/198; both files untouched until your reply.

wait_for_message implements the locked eventing contract: eligibility query, then a ctypes inotify watch on the instance DIRECTORY filtered to the mailbox.sqlite3* names (never a single WAL inode — checkpoints recreate it), then a REQUERY that closes the query→arm race, then a block bounded by the rescan interval; queue overflow, watch invalidation, directory replacement/move, and unmount all force full re-open validation before any rearm; every event is only a prompt to requery; inotify failure degrades to pure polling (pinned by monkeypatching the watch class away); a gated instance stands the waiter down with the gate's own diagnostic rather than spinning; timeout returns clean EXIT_NONE. The late-send pin proves watch-driven wakeup well inside a deliberately long rescan interval. Observability: doctor is a read-only diagnosis — integrity_check, foreign_key_check, gate/instance state, message-state counts, active claims, op_context leak detection, stale .init-/.copy- scratch inventory, and unrecognized-file report, exiting non-zero on problems; dump is a byte-redacted snapshot of every protocol table plus the transitions tail; materialize re-emits a durable body as a byte-exact idempotent projection through the no-clobber publisher and refuses scrubbed bodies. The CLI is a thin argparse layer over every existing transaction API — init, regen, send (stdin/file bodies, ROOT:PATH attach, XOR enforced by the store), send-notice, claim, wait, see, expire, reply, close, recover-claim, gc, scan, doctor, dump, inspect, materialize, the maintenance/move ceremony set, and migrate — printing JSON and mapping BatonError to the documented exit-code table; the CLI matrix pins the full send→claim→reply roundtrip, none-eligible=3, gated=7, and missing-config=4. Remaining after this review: packaging (zipapp + bootstrap floors + DISTRIBUTION manifest + extraction-purity isolated-checkout harness) and the coordinated cutover preparation. PROGRESS.md updated.
