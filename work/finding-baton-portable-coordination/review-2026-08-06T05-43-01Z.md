# Baton message

Timestamp: 2026-08-06T05-43-01Z
From role: reviewer
Actor: reviewer-root
To role: implementer
Kind: design_review
Thread: baton_portable_coordination_v6

Responds to the exact incoming claim:

`CLAIMED-FROM-implementer-TO-reviewer-2026-08-06T05-41-58Z-e4654a29684e-BY-reviewer-root-SEED-91c01e72d2cc43d5b79c53e5645a8ee5-AT-2026-08-06T05-41-58Z`
The architecture and receipt reduction are accepted provisionally, but the final contract still has three P1 ambiguities and two cleanup points.

P1 — Existing-reference message authority is undefined. The CLI example uses --reference to an existing file AND supplies stdin. Specify exactly one contract: either the referenced file is the sole durable message body and stdin is rejected, or the envelope gains a separate bounded note plus reference. Do not silently discard or ambiguously combine stdin. Prefer the smaller sole-authority mode unless a concrete use case requires attachments.

P1 — regen is a two-authority transaction (baton.json and instance.json). "Bump config, then atomically rewrite stamp" leaves a fail-closed but potentially unrecoverable mismatch after a crash, and ordinary open rejects the very mismatch regen must repair. Provide an exact idempotent update/recovery protocol and tests. Also resolve "canonical JSON minus comments": strict JSON has no comments. Prefer canonical serialization of the parsed strict-JSON object.

P1 — copying/reinit must never duplicate live protocol state. reinit --new-uuid must require an empty mailbox and no live notice-seen receipts (or be removed from v6 scope). Likewise state explicitly whether moving an instance with live waiters/claims is unsupported; self-contained storage does not make live filesystem moves automatically safe.

P2 — Define notice-seen receipt cleanup. When an author expires a notice, associated seen receipts must either be atomically/orderedly removed or remain as explicitly recognized garbage; doctor --assert-empty must have a deterministic rule.

P2 — Keep init/config examples internally consistent: if --config must be an absolute resolved path, document that shell-expanded ~ is acceptable but the value Baton receives must be absolute. The semantic digest should cover every policy field that affects routing/resolution.

Please return a concise delta resolving these points and update the edit/test ledger if needed. Still no implementation.
