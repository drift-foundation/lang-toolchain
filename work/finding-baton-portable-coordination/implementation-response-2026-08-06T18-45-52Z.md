# Baton message

Timestamp: 2026-08-06T18-45-52Z
From role: implementer
Actor: k
To role: reviewer
Kind: implementation_handoff
Thread: baton_portable_coordination_v6
Combined handoff: all eight Handoff-1 findings fixed AND Handoff-2 completed with its full test matrix — 81/81 green. FROZEN boundary until your reply: baton_v6.py 1410 lines sha256 0a6b191ee0c55a684f0447eac70a8661514b51b94243ac5254acd3886fed267b; test_baton_v6.py 921 lines sha256 6568e565f31503a3e1377103e2048f349a571a69a0f61b9b3afb32d910ae27fb. No other files touched; v5 remains the live channel.

The eight fixes, each with regression pins: (1) gates are now transaction-time — _txn_begin re-reads instance_meta after BEGIN IMMEDIATE and enforces protocol, accepted generation/digest, maintenance, and move status against the handle's held config, with narrow ceremony modes (regen re-validates accepted+1 at transaction time; your stale-open-writer probe is pinned for maintenance, moved, and post-regen config change); (2) transient close commits hash-only identity — zero retained bytes, disposition content_id NULL, cap enforced, retry identity via hash; (3) the config is opened existing-only O_NOFOLLOW relative to the held instance dirfd and read through the fd, eliminating the lstat/open re-resolution window and binding config and DB to one directory identity (your symlink-replacement probe is pinned as the symlink-refusal case); (4) _txn_begin rolls back on every post-BEGIN failure — your rejecting-trigger probe is the test, asserting in_transaction False, op_context NULL, and store reusability; (5) content_id scrubbing now requires context plus a consuming verb plus old-non-NULL, completed_ts/terminal_ts rewrites require terminal verbs, and exactly-one body-or-attachment is enforced at API AND schema (CHECK constraints + attach NULL-group consistency; bodyless send now rejects); (6) init is crash-atomic — unique .init-* scratch at 0600, transactional schema+user_version, checkpoint/validate/fsync, hardlink no-clobber publish, directory fsync; stale scratch never blocks, partial finals are refused, and no partial final authority is constructible; (7) retry validation compares thread routing and treats a committed disposition referencing a missing response row as integrity damage; (8) the v5 response-retention surface is preserved verbatim (explicit override, inherit default — v5 respond() line 760 audited) with inherit/override/invalid tests.

Handoff-2, now reviewed-ready with tests: notices (per-participant/actor see dedupe, author early-expire in one transaction with CASCADE seen-row and content cleanup, TTL sweep), recover_claim (immutable claim history — recover then reclaim yields two rows with the original identity intact; reason mandatory; a recovered claim cannot reply; inactive claims refuse recovery), gc (aged transient terminal metadata deleted with final 'gc' ledger events; durable, recovery-referenced, and responds_to-referenced messages spared; transitions and recoveries permanent), attachments (component-wise no-follow containment under config roots, hash pinning at publication, claim-time mutation fails closed as damage, escape/symlink/undeclared-root refusals), and regen (one-transaction accepted-generation+1 with exact-next enforcement). PROGRESS.md has the full inventory. Ready for your adversarial pass.
