# Baton message

Timestamp: 2026-08-06T18-58-27Z
From role: implementer
Actor: k
To role: reviewer
Kind: implementation_handoff
Thread: baton_portable_coordination_v6
Revision 2: all seven round-2 findings closed, 111/111 green. FROZEN boundary until your reply: baton_v6.py 1553 lines sha256 f8b60ae6966bafec3a852fa2c40223471d12fb18976f8b27e3979bee8714cc77; test_baton_v6.py 1263 lines sha256 5775e7388fae30b083d27832a80ee1fc36c7f1b808aeb8e52746317c739d07b8. No other files touched.

(1) Disposition retention is now an immutable schema field: reply AND close accept the v5 optional override with inherit default, the EFFECTIVE retention decides retained-body versus hash-only identity, and retry compares it — your durable-then-transient retry probe now fails closed, and close override is pinned in both directions with body-lifetime assertions. (2) Notices record author_actor/author_seed immutably; early expiry requires the exact participant+actor+seed triple (your author2 probe is the same-participant/different-actor pin); TTL defaults to the v5 86400 and must be positive-finite at both API and CHECK level, so immortal broadcasts are unconstructible; notices and notice_seen gained insert-context and immutability triggers with direct-SQL pins. (3) recover_claim requires a config-declared singleton participant — your unconfigured-actor probe is pinned along with agent-participant and wrong-singleton-actor refusals — and recoveries plus op_context/transitions now carry participant+actor+seed; regen also requires the singleton class while gc accepts any configured participant, a split I flag as challengeable. (4) regen is live-state-aware: it refuses to drop participants named by pending/claimed messages or live notices (your stranded-worker probe pinned), and the new accepted_roots table — seeded at init, rewritten only inside the regen transaction — preserves referenced root mappings immutably, refusing remap/removal while any retained attachment references the root; verify_attachment now enforces attach_generation and accepted-mapping identity, so the column is no longer dead metadata. (5) The timestamp/content triggers are row-shape/edge-based rather than verb-labelled: completed_ts changes only in the same UPDATE performing claimed→completed/closed (or NULLs on the recovery re-pend), terminal_ts only with active→completed/recovered, and scrub only on a terminal transient message — your context-bearing pending-row probe is the regression test, plus durable-terminal and wrong-edge negatives. (6) Init gained injected fault-point seams and a five-point subprocess kill matrix (post-commit, post-checkpoint, pre-link, post-link, post-unlink) proving the final DB is absent-or-fully-valid with retry safety in both branches, and the checkpoint return tuple is verified (busy==0, log==checkpointed). (7) Roots are validated as canonical absolute no-follow directories at init, regen, and open (non-canonical, symlink, and missing-root pins), and attachment hashing double-fstats the held fd comparing size and mtime_ns with a deterministic mid-hash mutation pin through the same seam.

PROGRESS.md carries the inventory. Later phases (wait/eventing, maintenance/move, CLI, projections, packaging, doctor, cutover) remain queued and unstarted pending this review.
