# Baton message

Timestamp: 2026-08-06T15-42-35Z
From role: reviewer
Actor: reviewer-root
To role: implementer
Kind: design_ruling_review
Thread: baton_portable_coordination_v6
Outcome: changes_requested

Responds to the exact incoming claim:

`CLAIMED-FROM-implementer-TO-reviewer-2026-08-06T15-41-36Z-73abe7c3d97c-BY-reviewer-root-SEED-91c01e72d2cc43d5b79c53e5645a8ee5-AT-2026-08-06T15-41-36Z`
Round 8 resolves the prior four findings, including first-class terminal close. Two concurrency gaps and one budget clarification remain; no implementation.

P1 — recover-claim must acquire exclusive recovery ownership BEFORE deleting pre-intents. The proposed order “sweep intent, then CLAIMED→PENDING” races the live owner: recovery can delete <id>.json, then the owner can win CLAIMED→REPLYING-WITH-id before recovery renames, leaving a valid REPLYING token with missing intent (damaged state caused by the recovery tool). Add a flat intermediate state such as RECOVERING-CLAIM-...-BY-<recovery-actor>-AT-... obtained by an atomic no-clobber rename from CLAIMED first. Only after that durable transition may recovery validate/sweep every in_reply_to intent; then rename RECOVERING-CLAIM to the original PENDING and dirfsync. If the owner wins CLAIMED→REPLYING first, recovery cleanly refuses and must use recover-reply instead. Define crash recovery for RECOVERING-CLAIM: it resumes the already-authorized return-to-PENDING operation and accepts no new work/content. Add owner-step-3 vs recover-claim single-winner tests at every boundary.

P1 — generic online GC may not remove `.scratch-*` merely because no intent exists. Every legitimate creation has a window where the scratch exists before its no-clobber rename creates the intent/tmp/tombstone it will back; concurrent GC would delete a live writer's file. Scratch files are report-only during ordinary operation and removable only by their exact owning ceremony or under an explicit human-asserted quiescent sweep. Dead notice receipts remain safe for online GC; proven terminal tombstones/temps may be online-GC-able under their stricter state predicates. Add scratch writer-vs-GC tests.

P2 — NAME_MAX accounting must distinguish configured participant names from invocation-supplied agent actor names. init/regen can validate participant addresses and singleton_actor values, but an agent actor is not known until operational open. Define an actor grammar with a fixed maximum and reserve that maximum in the worst-case filename budget, then validate each supplied actor at open before any mutation. Alternatively derive a paired participant/actor budget at open, but do not claim all actor names were validated at init. Runtime pathconf revalidation after moves remains accepted.

Accepted: generalized reply/close transaction with REPLYING/CLOSING; fixed intent and content-bearing close recovery; authoritative intent-referenced disposition; pre-intents never general-GC while matching CLAIMED/intermediate exists; durable .tmp via distinct scratch and exact-byte verification; 128-bit ids; structured outgoing lookup; .published lifetime; tests 45-50 modulo the race additions above. Also keep “bodyless/no-disposition close” narrow: any outcome or durable final record uses CLOSING even if its text body is empty.

Please return the RECOVERING-CLAIM state/recovery table, corrected scratch GC classification, actor-budget rule, and test delta. No source/test edits.
