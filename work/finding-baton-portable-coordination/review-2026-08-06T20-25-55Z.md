# Baton v6 maintenance/move/migrate slice sign-off

Timestamp: 2026-08-06T20-25-55Z
From: reviewer
To: implementer
Thread: `baton_portable_coordination_v6`

Signed off for the frozen round-9 pair:

- `tools/baton/baton_v6.py`: 2508 lines, sha256 `2469e149299494780b3cc6d588182dcecb7057c589d56d6036608eb7a6558e06`
- `tools/baton/test_baton_v6.py`: 2522 lines, sha256 `f70554032ee9a28032eeca9e2481770d9a28be08f983c96dde3749643a325e82`

The hashes/counts match, `py_compile` is clean, and the 186-test focused suite independently exits green. Static review confirms that the post-publication pair now consumes the same immutable destination binding as stage discovery, live mirrors/role are checked before the first `copied` result, source config replacement is rejected as non-regular before reading, and destination artifact open failures use the normal Baton error surface.

The maintenance/move/migrate slice is clear. The final model has one immutable move-binding authority, exact no-follow source/destination identity, role-specific transitions, activation-gated decommission, exact-token/routing retry validation, bounded-memory resumable copying, and lost-response crash recovery. The reviewed repros no longer produce multiple active authorities through the API.

Proceed to the remaining authorized v6 work: CLI over the existing transaction APIs, wait/eventing, doctor/scan/dump/materialize, then packaging/cutover and extraction-purity gates in the PLAN's order. Update the stale PLAN status line from “implementation NOT authorized” to reflect Slawomir's explicit authorization; do not otherwise reinterpret the accepted plan. Preserve red-first/fault-injection discipline and return the next phase as one frozen boundary.
