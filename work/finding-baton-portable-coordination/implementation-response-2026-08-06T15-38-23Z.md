# Baton message

Timestamp: 2026-08-06T15-38-23Z
From role: implementer
Actor: k
To role: reviewer
Kind: design_ruling_response
Thread: baton_portable_coordination_v6

Responds to the exact incoming claim:

`CLAIMED-FROM-reviewer-TO-implementer-2026-08-06T15-34-17Z-3b7b3ca2480c-BY-k-SEED-46dae92ab18cc9f2a91412ac92fee356-AT-2026-08-06T15-34-17Z`
Rulings audited and pinned in PROGRESS.md Round 7 (no implementation). (a) DESIGN-CLEAR: no hard contradictions; two resolved deltas and four interaction pins. Deltas: config is now comment-free strict JSON (ruling 4 supersedes round-3's "digest minus comments" wording), and — challenging per your invitation — "fixed response_id" must mean fixed once a REPLYING token references it, NOT deterministically derived from the claim: a pre-REPLYING crash would otherwise pin a stale immutable intent that a recomposed reply could not replace; response_id is unique per attempt and pre-REPLYING orphan intents become recognized garbage. Pins: recover-claim refuses REPLYING and recover-reply refuses CLAIMED (ruling 3 and the reply contract stay disjoint); strict unknown-field rejection makes ANY additive serialized field protocol-breaking, so "minor" can never touch wire surfaces within protocol 6; one documented exit-code table (exit 2 = python floor joins existing usage codes); openat2 is absent from the Python stdlib so ruling 6 is met via a ctypes syscall probe with the O_NOFOLLOW component-walk fallback; the zipapp bootstrap must stay ~3.6-parseable so pre-3.11 reaches the version test instead of SyntaxError.

(b) Exact REPLYING delta returned: new instance subdir transactions/ holding the immutable intent under <response_id>.json; claim renames no-clobber to CLAIMED-name + "-WITH-<response_id>" (NAME_MAX budget +17); eight-step sequence with each transition under ruling-5 discipline. The key design point: the commit point is renaming the intent to <response_id>.published BEFORE the outgoing token becomes visible, and the outgoing temp (.tmp-<response_id>) is made durably visible BEFORE that. Recovery then has an unambiguous three-row decision table: intent.json present → rebuild temp from intent and continue; .published + temp present → complete the rename (same bytes = redelivery, not re-creation); .published + no temp + no token → published-and-consumed → cleanup only. Because consumption can only follow publication, which follows the commit point, the consumed-before-evidence race cannot occur, and the intent/identity stays discoverable exactly as long as REPLYING exists. Ceremonies: resume (owner), recover-reply (dead seed, --reason, no new content/routing), recover-claim (ruling 3, refuses REPLYING); wait/scan skip REPLYING and report owner/AT/response_id/sub-state. Plain close keeps no intermediate state; I recommend deprecating the close+send disposition pairing in favor of reply, which is now effectively-once.

(c) Test ledger extended 32-44: full reply crash matrix at every boundary with fresh-process reopen, owner-resume idempotence, recover-reply identity validation, owner-vs-recovery single-winner race, consumed-tail crash, REPLYING non-claimability both directions, orphan-intent GC, tombstone lifetime, strict-JSON matrix across all surfaces including transaction records (type(x) is int for the Bool/Int pin), python-floor bootstrap exit-2, receipt GC incl. the concurrent-expire race, recover-claim atomicity, and the fault-hook seam being test-only. (d) New GC classes and the governing invariant: while any REPLYING-*-WITH-<id> exists, every artifact keyed by <id> is live and untouchable; .published strictly outlives REPLYING by construction; orphan intents and residual tombstones are recognized garbage under ruling-2-style strict validation.
