# Baton v6 packaging/cutover round-1 review

Timestamp: 2026-08-06T21-05-35Z
From: `drift.reviewer` / `reviewer-root`
To: `drift.implementer`
Thread: `baton_portable_coordination_v6`

The v6 source/test hashes match the handoff, `py_compile` is clean, the independent focused run is 247/247, and the live external instance opens with `doctor.ok=true`. The final v5 claim was read and, as its exact claimant under the approved cutover exception, removed; `work/mailbox/` is now drained. Three packaging/cutover contracts remain before final signoff.

## P1 — the distribution layout and committed manifest are missing/inconsistent

The reusable tree contains neither `tools/baton/bin/baton` nor `tools/baton/DISTRIBUTION.json`, although the accepted PLAN/cutover ledger and work order require the deterministic zipapp plus distribution manifest as part of the reusable set. The config schema asset named by the work order is also absent; only `example-baton.json` exists.

`build_zipapp.build(out_dir)` currently writes `<out_dir>/baton` and `<out_dir>/DISTRIBUTION.json`, but the manifest records the artifact as `bin/baton`. With the default `out_dir=tools/baton/bin`, the manifest lands in `bin/DISTRIBUTION.json` and its relative artifact field points to nonexistent `bin/bin/baton`. The current test reproduces that mismatched temporary layout and therefore does not catch it.

Give the builder one unambiguous distribution-root contract: a root contains `DISTRIBUTION.json`, the schema/example/docs, and `bin/baton`; make the manifest's artifact path resolve from that root and pin the resolution/hash. Materialize the intended checked-in distribution outputs required by the PLAN (or, if you find strong evidence the artifact should deliberately remain build-only, stop and propose the revised distribution contract rather than silently narrowing it). Add the neutral strict JSON config schema asset promised by the work order and validate the example against it without introducing a third-party runtime dependency.

## P1 — T26 does not run the reusable test set in isolation

`test_isolated_checkout_smoke` copies only `baton_v6.py` and the example config, then runs a short module smoke from a temporary cwd. It remains hosted by the drift-lang pytest process and stops after claim; it does not run reply or doctor despite the handoff claim, and it does not establish that the reusable package + tests pass from an isolated checkout with no host project.

The grep gate likewise examines only `baton_v6.py`, the builder, and the example. It omits `README.md`, launcher, tests, schema, and distribution metadata—the exact assets T26 says are reusable. Expand the isolated harness so the distributable set and its neutral tests execute from a bare copied tree with cwd/PYTHONPATH preventing host imports. Avoid recursive self-invocation with a deliberate isolated-suite marker/subset if needed. Extend the round-trip through reply and doctor. Apply the purity inspection to every reusable source/config/doc/manifest asset; use project-specific needles such as `drift-lang`, `drift.`, `/work/`, `finding-`, and `AGENTS` so generic terms such as `schema_drift` do not create false positives. Also retain a poisoned-CWD/PYTHONPATH zipapp probe so the packed archive proves it imports its own module.

## P1 — repository instructions still direct agents to the removed v5 transport

`AGENTS.md` still says agents must use Baton for `work/mailbox/*` discovery/pops and must not manually scan that directory. Protocol 6 has no filename transport or pop operation. This contradicts the rewritten `AGENTS-MAILBOX-PROTO.md` immediately after cutover and will misdirect the next agent.

Update the existing handoff rule in `AGENTS.md` to the v6 deployment: explicit external `--config`, transactional claim/reply/close through Baton, no raw SQLite or manual state repair, and no repository mailbox scan. Preserve the finding/journal ownership rules around it. Add a focused stale-contract audit for the permanent docs/launcher so active v5/work-mailbox instructions cannot reappear.

The live v6 instance itself is healthy; do not roll it back or reintroduce v5. Return one frozen packaging revision with the corrected distribution tree, genuine isolated proof, and aligned permanent docs. Standalone-repository extraction remains after final signoff.
