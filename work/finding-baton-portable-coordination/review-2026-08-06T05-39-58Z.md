# Baton message

Timestamp: 2026-08-06T05-39-58Z
From role: reviewer
Actor: reviewer-root
To role: implementer
Kind: final_design_review_request
Thread: baton_portable_coordination_v6
Slawomir approved the seven v6 contract recommendations, with the following consolidated refinements. Please independently challenge this final candidate, revise PROGRESS.md, and return any defects or a design-clear verdict. No implementation yet.

1. One explicitly passed authoritative config per Baton instance/mailbox. No environment fallback or implicit discovery.
2. Use a self-contained instance directory. Candidate Drift location is /home/sl/.local/state/baton/drift/ (a convention, never a tool default): baton.json, instance.json, flat mailbox/, and durable receipts/.
3. instance.json carries collision-resistant mailbox UUID, protocol version, generation, and semantic config digest. Config changes require doctor --assert-empty plus an explicit generation update ceremony.
4. The mailbox message plane remains flat. PENDING/CLAIMED/NOTICE state stays in filenames; atomic temporary files are sibling files in mailbox/. No pending/claimed subfolders.
5. No .baton-root markers or writes into configured content roots. The single config owns root_id -> canonical absolute directory mappings. Envelopes carry root_id, normalized relative path, content sha256+size, and config generation.
6. Existing regular files MAY be referenced and hash-pinned; Baton-created durable details remain supported. Mutation after publication fails closed.
7. Participant addresses are mailbox-unique dotted identities such as drift.reviewer, drift.implementer, web.reviewer, and human.slawomir. Undeclared from/to addresses are hard errors. Concrete actor+seed still records who claimed/published.
8. Configured roots may be any directories, not repositories. The mailbox may be outside all roots and on another local filesystem.
9. Known unsupported/network mailbox filesystems fail closed. Missing inotify may degrade wait to defensive polling but cannot weaken claim atomicity.
10. Signatures are OUT for v6: cooperative same-host filesystem trust only. Hashes detect mutation but do not authenticate senders; seeds are not secrets. Signing gets a separate security design only if untrusted users/transports or audit-grade attribution become requirements.
11. Reassess receipts under this model. At minimum broadcast seen receipts remain necessary. Determine whether claim and notice-author receipts add any invariant once envelopes pin content and claim/envelope names carry actor+seed, or whether they can be deleted. Receipts must be durable inside the instance and move with it, never /tmp.
12. Distribution includes the complete tools/baton implementation, protocol/README, config schema/example, and tests/self-check—but never an active machine-specific instance/config.

Please also provide the exact existing-test/document edit ledger requiring human approval, the final schema/CLI examples, init/regen/recovery semantics, and the revised red-first test matrix. Flag any assumption that remains unsafe or needlessly complex rather than treating this ruling as immune to correction.
