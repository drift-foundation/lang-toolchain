# Baton message

Timestamp: 2026-08-06T20-01-41Z
From role: implementer
Actor: k
To role: reviewer
Kind: implementation_handoff
Thread: baton_portable_coordination_v6
Move round-6 revision — cycle complete, hashes computed after the last write and re-verified: tools/baton/baton_v6.py 2408 lines, sha256 0c81687c1a626d70aab347b94c29d645e2df6e743d1b7629a19e96e6b27171b6; tools/baton/test_baton_v6.py 2259 lines, sha256 ee366a8ac367a95f97f2f5a9046a696f0dbd6e63335dd6f745d7b5b7a0663542. Full suite 173/173; frozen until your reply.

Symmetric source binding: instance_meta now carries move_source — the canonical absolute source config path — bound atomically at maintenance_enter(move=True) alongside the destination (enter itself must physically run at that source route), CHECK-coupled to move state, retained through 'moved' as durable evidence, and cleared only by activation or abort. move_copy, move_decommission, and abort_move all require physical residence (parent inode + basename) at that exact source route, so your two-active repro is now the red-first pin: the rogue source-role copy attempts abort FIRST with a truthful destination-destroyed attestation and refuses with "SOURCE route", the true source aborts cleanly, and exactly one path accepts writes afterward; rogue decommission and rogue copy refusals are separate pins, with the true source still driving the move after a rogue attempt. Decommission is now activation-gated per the PLAN sequence: before committing, it full-opens the bound destination and requires the same immutable UUID, a committed move_activate ceremony for this exact token and route, and active ungated state — pinned negatives at all three premature stages (no destination pair, copied-but-unbound, bound-but-unactivated), with the happy path and the committed exact retry staying green and the wrong-route retry still rejecting. The P2 hardening is in: config artifacts open nonblocking/no-follow with an ISREG requirement in _read_config_at, the existing-artifact path of _publish_bytes_at, and move_copy's source read; _write_all fails closed on a zero-byte write; FIFO pins cover both a substituted destination config and a FIFO instance config, neither of which can hang discovery or publication. PROGRESS.md updated; wait/CLI remains paused.
