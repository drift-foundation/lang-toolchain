# Baton message

Timestamp: 2026-08-06T19-20-30Z
From role: implementer
Actor: k
To role: reviewer
Kind: implementation_handoff
Thread: baton_portable_coordination_v6
Revision 3: all six round-3 findings closed plus the extraction wording — 133/133 green. FROZEN boundary until your reply: baton_v6.py 1680 lines sha256 2c15890b7a85d19d8f7ce88a608c7e8bb5c3fccbd668cecbd39fd71d3e2a7e1a; test_baton_v6.py 1551 lines sha256 cb34ec331ef40d9409461f688c1c09f23f0fd83a383202ee729430e64ebfed51.

(1) Reply now normalizes the effective recipient and thread from the incoming row BEFORE the disposition lookup and validates them unconditionally on retry — None means inherit on both first publication and retry, never wildcard — with participant validation only on the fresh path so historical retries survive safe config changes; your explicit-then-omitted probe is pinned in both directions plus the default-route positives. (2) The coupling is now bidirectional at constraint level: messages carry CHECK((state IN ('pending','claimed')) = (completed_ts IS NULL)) and claims CHECK((state='active') = (terminal_ts IS NULL)) — your timestamp-less terminal transitions and prefilled births are all rejected by the schema, with the edge triggers retained as diagnostics. (3) gc is a retention-graph fixpoint per your suggested shape (accepted; one correction of my own along the way: the children-first deletion order originally used created_ts, which ties within one second — it now derives from the responds_to references themselves): candidates minus recovery-anchored, iteratively minus anything anchored by a retained responds_to child or a retained claim's disposition response reference; deletion runs dispositions→claims→messages children-first→contents. The pinned contract: a transient response anchored by a RETAINED durable disposition remains retained as metadata (the immutable disposition is the retry-identity authority); all-transient chains collect fully; transient-incoming/durable-response chains are fully retained; gc never aborts on a valid graph, makes bounded progress, and a post-gc retry fails clean with "unknown claim" while the ledger survives. (4) The no-follow boundary is now a component walk from an opened "/" dirfd with O_DIRECTORY|O_NOFOLLOW per component, shared by the instance directory and configured roots — your intermediate-symlink probe is pinned for both. (5) notice_seen has an unconditional update-abort and expire/gc-context-only delete triggers with the expire-cascade positive asserted, and recoveries has an insert-context guard — your forged-receipt and forged-recovery probes are the tests; raw uncontextual SQL writers are not a supported boundary. (6) The attachment snapshot compares (dev, ino, mode, size, mtime_ns, ctime_ns) across the held fd — your same-size/restored-mtime probe is pinned, caught by ctime — and the leaf opens O_NONBLOCK so a FIFO rejects at the regular-file check without blocking. Module docstrings now read capability-authorized.

Later phases (maintenance/move/migrate, wait/eventing, CLI/projections, packaging/doctor, cutover) remain queued. PROGRESS.md updated.
