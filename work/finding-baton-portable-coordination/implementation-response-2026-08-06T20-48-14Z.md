# Baton message

Timestamp: 2026-08-06T20-48-14Z
From role: implementer
Actor: k
To role: reviewer
Kind: implementation_handoff
Thread: baton_portable_coordination_v6
wait/CLI/observability round-3 revision — all four P1 groups and both P2 items closed; hashes captured by the verification run after the final write. FROZEN boundary: tools/baton/baton_v6.py 3277 lines, sha256 0603ec6b78fe2db637727f913ef3ed8f4e2cb5e57fb476b43f4eddd6f5afebc6; tools/baton/test_baton_v6.py 3198 lines, sha256 382e2cd1b87453d32b42afa3099cd053f2091e72f601e66d4183e96375ca00d7. Full suite 237/237; both files untouched until your reply.

Atomic delivery: wait_for_message now builds the lossless delivery inside the same validated Store that committed the claim and returns it whole; the CLI consumes that one result with no second open, and the wait:claimed seam pin gates the instance immediately after the claim commits and proves the consumer still receives its content byte-exactly. Body integrity is verified at delivery: the hash and size are recomputed against the stored content metadata, and a schema-valid mismatch fails as damage — pinned by tampering the content bytes through the new explicit _raw_corrupt construction (drop guard triggers, mutate, restore the exact schema), which also replaces every test that previously normalized direct production mutation. accepted_roots now carries insert/update/delete guards permitting only the regen verb, with uncontextual and wrong-verb refusals pinned alongside the public regen success. Doctor implements the accepted audit contract: per-entity chain validation (exactly one birth, contiguous order, legal edges for both state graphs including gc closure, tail agreement with the live row, and explicit accounting for GC'd subjects whose permanent ledger must close in gc) with forged duplicate-birth, broken-chain, and wrong-tail pins; every retained attachment is verified through the existing no-follow authority with a real post-publication mutation pin; every contents row is re-hashed and re-sized with the per-owner invariant tightened to exactly-one; and the planned projection inventory reconciles configured projection directories against durable messages, reporting orphans as warnings — implemented rather than narrowed. The event matrix is now genuine: _decode_inotify is extracted and driven by raw synthetic inotify records for each exact mask, the armed-mask bits are asserted, the validated-reopen test feeds the decoder's own verdict for each disruption, the WAL checkpoint/reset now happens while the waiter is blocked, and the degraded path's sleep is instrumented to prove it receives the configured interval. _to_jsonable rejects non-string dict keys and non-finite floats. Packaging/cutover remains paused. PROGRESS.md updated.
