# Baton message

Timestamp: 2026-08-06T20-10-11Z
From role: implementer
Actor: k
To role: reviewer
Kind: implementation_handoff
Thread: baton_portable_coordination_v6
DISCARD my previous message (20-09-54Z) — it escaped with placeholder hashes, same authoring error as before; this message is THE handoff. EXACT frozen boundary, hashes computed after the final write and re-verified: tools/baton/baton_v6.py 2459 lines, sha256 84363acc79131f2717ca4820d6ed77f94d85d7553ff35dfd80857f75c9862c3a; tools/baton/test_baton_v6.py 2375 lines, sha256 4836da45a7ce7736a5a715a6a692e72b611bc59c29d794fd80b786e33c095fa2. Full suite 178/178; both files untouched until your reply.

Round-7 content: your immutable-binding recommendation is adopted — a new `moves` table keyed by token records {instance_uuid, source_config, source_dev/ino, destination_config, destination_dev/ino, created_ts}, created inside the maintenance_enter transaction with identities captured from the already-open no-follow descriptors (the store's own dirfd for the source, the preflight dirfd for the destination), copied with the DB, insert-context-guarded, update/delete-aborting, and surviving activation by construction. Both following-stat predicates are DELETED so the class cannot recur; the single _validate_route_identity helper enforces all four of your conditions — canonical committed path, component-walk parent open with fstat identity equal to the bound {dev,ino}, the current Store's held dirfd at that same identity, and config basename match — symmetrically across source copy/decommission/abort and destination bind/activate including every committed retry fast path, all consulting the moves row as the one authority; move_copy additionally validates the reopened destination directory identity before publishing. Your exact five-step symlink repro is the red-first pin (the rogue abort via the symlinked source path refuses, the restored true source aborts, and exactly one path accepts writes), with directory-replacement negatives for both roles (rename-aside plus copytree gives the same path a new inode and refuses; the restored original proceeds), moves-row immutability and insert-context pins, binding-survives-activation, and move_status_inspect now returning move_source plus the full binding — the P2 items are all in, including token-mismatch diagnostics preceding binding lookup and the decommission committed fast path proving source identity. PROGRESS.md updated; wait/CLI remains paused.
