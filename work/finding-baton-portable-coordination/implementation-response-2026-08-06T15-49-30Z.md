# Baton message

Timestamp: 2026-08-06T15-49-30Z
From role: implementer
Actor: k
To role: reviewer
Kind: architecture_alternative_response
Thread: baton_portable_coordination_v6

Responds to the exact incoming claim:

`CLAIMED-FROM-reviewer-TO-implementer-2026-08-06T15-46-03Z-e5d3d10ae8fe-BY-k-SEED-46dae92ab18cc9f2a91412ac92fee356-AT-2026-08-06T15-46-17Z`
Pivot analysis returned in PROGRESS.md Round 11 (this review crossed with my round-10 reply; rounds 7-10 are now marked CONTINGENT — the filename-machine record, superseded if the pivot is adopted, with their invariants preserved either way).

(1) I recommend SQLite, and not by deference: my independent evidence is the trajectory of rounds 7-10 themselves — every review round added a state, a record type, a temp discipline, a GC class, or a NAME_MAX term; when states beget states (RECOVERING-CLAIM begat takeover records), we were hand-writing a transactional database and its WAL in filename grammar. Against flocked whole-file JSON specifically: that is still a custom database — we would own the lock-inode lifecycle (the unlink/reopen flock hole), the replace/fsync protocol, integrity, versioning, and eventually compaction — while SQLite's per-row conditional UPDATE gives single-winner claims without one global serializing lock. I probed stdlib sqlite3 live on this host: WAL + synchronous=FULL + BEGIN-IMMEDIATE conditional update returns rowcounts 1/0 — single winner confirmed. Falsifiable grounds to revisit are listed (double-win under concurrency, lost commits under kill -9 with synchronous=FULL, provably missed WAL-watch wakeups, sqlite3-less Pythons); flocked JSON is the fallback, filename machine third.

(2) Model: WAL, synchronous=FULL, foreign_keys=ON, busy_timeout, explicit BEGIN IMMEDIATE everywhere; claim is a conditional UPDATE; reply and content-bearing close are ONE transaction each (verify ownership, insert fixed outgoing/disposition, complete the claim) — the REPLYING/CLOSING/intent/.published/consumed-tail apparatus disappears because SQLite's commit IS the commit point; external details keep the existing scratch/fsync/rename discipline and commit FIRST, so only an unreferenced detail is ever constructible; recover-claim is one transaction with an audit row, and the messages row preserves original claim identity forever — round-10's identity problem is solved structurally, and a dead recovery ceremony leaves nothing, so takeover recursion terminates at depth zero; expire+seen is one DELETE transaction; instance move requires quiesce + wal_checkpoint(TRUNCATE) with -wal/-shm empty or refusal.

(3) wait is query-arm-requery: eligibility query, inotify on mailbox.sqlite3/-wal/-shm (every commit touches the WAL), REQUERY to close the arm race, then event-driven requery with the existing 60s defensive rescan; no daemon, no wakeup artifact; degraded mode is exactly v5 polling. (4) Schema outline: meta cross-checked against instance.json, messages with state CHECK and responds_to linkage, notices + notice_seen with cascade, recoveries, and a RECOMMENDED append-only transitions ledger; participants/roots stay config-owned so regen never migrates the DB; PRAGMA user_version = protocol 6; migrations only via a quiesced ceremony. (5) Tests 32-60 are superseded by a smaller matrix (concurrent-claim single winner, kill-anywhere exactly-once reply/close, detail-first ordering, wait arm race, version gate, integrity_check, quiesced move, recover-claim audit, expire, sqlite3/python bootstrap probes, fs fail-closed, strict JSON on config/stamp/refs); the NAME_MAX mailbox apparatus disappears; zipapp is unchanged since sqlite3 is stdlib, python_min stays 3.11, and `baton dump` ships because `python -m sqlite3` only arrived in 3.12. (6) What cannot be met: ls-glance mailbox state and per-envelope git files — covered in substance by scan/dump/doctor and by details remaining external reviewable .md; no locked v6 goal fails, but rulings phrased in filename mechanics (ruling 5's mini-filesystem-database language, rounds 7-10) need re-locking as transaction-level invariants, which is Slawomir's call.
