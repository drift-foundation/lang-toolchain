// GENERATED by gen_small.py — do not edit by hand.
module main;

import std.core as core;
import std.text as text;
import std.regex as regex;

// byte-exact builders (mirrored in model.py — keep in sync via
// gen_small.py only)
fn mk_fill_sm(n: Int) nothrow -> String {
	var sb = text.string_builder(n + 32);
	var len = 0;
	while len + 28 <= n {
		text.sb_append_string(&mut sb, &"worker,tunnel,socket,buffer,");
		len = len + 28;
	}
	while len < n {
		text.sb_append_string(&mut sb, &"x");
		len = len + 1;
	}
	return text.sb_build(&mut sb);
}

fn mk_early_sm(n: Int) nothrow -> String {
	var sb = text.string_builder(n + 32);
	text.sb_append_string(&mut sb, &"id42,");
	var len = 5;
	while len + 28 <= n {
		text.sb_append_string(&mut sb, &"worker,tunnel,socket,buffer,");
		len = len + 28;
	}
	while len < n {
		text.sb_append_string(&mut sb, &"x");
		len = len + 1;
	}
	return text.sb_build(&mut sb);
}

fn mk_late_sm(n: Int) nothrow -> String {
	var sb = text.string_builder(n + 32);
	var len = 0;
	while len + 28 <= n - 5 {
		text.sb_append_string(&mut sb, &"worker,tunnel,socket,buffer,");
		len = len + 28;
	}
	while len < n - 5 {
		text.sb_append_string(&mut sb, &"x");
		len = len + 1;
	}
	text.sb_append_string(&mut sb, &",id42");
	return text.sb_build(&mut sb);
}

fn mk_altlate_sm(n: Int) nothrow -> String {
	var sb = text.string_builder(n + 32);
	var len = 0;
	while len + 28 <= n - 6 {
		text.sb_append_string(&mut sb, &"worker,tunnel,socket,buffer,");
		len = len + 28;
	}
	while len < n - 6 {
		text.sb_append_string(&mut sb, &"x");
		len = len + 1;
	}
	text.sb_append_string(&mut sb, &",patch");
	return text.sb_build(&mut sb);
}

pub fn smk_alt_256() nothrow -> String {
	return mk_altlate_sm(256);
}
pub fn smk_alt_4096() nothrow -> String {
	return mk_altlate_sm(4096);
}
pub fn smk_early_256() nothrow -> String {
	return mk_early_sm(256);
}
pub fn smk_early_4096() nothrow -> String {
	return mk_early_sm(4096);
}
pub fn smk_fill_1024() nothrow -> String {
	return mk_fill_sm(1024);
}
pub fn smk_fill_128() nothrow -> String {
	return mk_fill_sm(128);
}
pub fn smk_fill_256() nothrow -> String {
	return mk_fill_sm(256);
}
pub fn smk_fill_4096() nothrow -> String {
	return mk_fill_sm(4096);
}
pub fn smk_fill_512() nothrow -> String {
	return mk_fill_sm(512);
}
pub fn smk_fill_64() nothrow -> String {
	return mk_fill_sm(64);
}
pub fn smk_late_1024() nothrow -> String {
	return mk_late_sm(1024);
}
pub fn smk_late_128() nothrow -> String {
	return mk_late_sm(128);
}
pub fn smk_late_256() nothrow -> String {
	return mk_late_sm(256);
}
pub fn smk_late_4096() nothrow -> String {
	return mk_late_sm(4096);
}
pub fn smk_late_512() nothrow -> String {
	return mk_late_sm(512);
}
pub fn smk_late_64() nothrow -> String {
	return mk_late_sm(64);
}
pub fn sc_compile_p1(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => { return 1; },
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_compile_pa(s: String) nothrow -> Int {
	match regex.compile(&"^[a-z,x]+$") {
		Ok(re) => { return 1; },
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_compile_palt(s: String) nothrow -> Int {
	match regex.compile(&"(get|post|put|delete|head|patch)") {
		Ok(re) => { return 1; },
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_late_64(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_late_128(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_late_256(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_late_512(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_late_1024(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_late_4096(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_nomatch_64(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_nomatch_128(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_nomatch_256(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_nomatch_512(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_nomatch_1024(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_nomatch_4096(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_early_256(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_early_4096(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_anchored_256(s: String) nothrow -> Int {
	match regex.compile(&"^[a-z,x]+$") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				if regex.is_match(&re, &s) { acc = acc + 1; }
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_anchored_4096(s: String) nothrow -> Int {
	match regex.compile(&"^[a-z,x]+$") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				if regex.is_match(&re, &s) { acc = acc + 1; }
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_alt_256(s: String) nothrow -> Int {
	match regex.compile(&"(get|post|put|delete|head|patch)") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_alt_4096(s: String) nothrow -> Int {
	match regex.compile(&"(get|post|put|delete|head|patch)") {
		Ok(re) => {
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first(&re, &s) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_late_view_256(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			val v = text.byte_view_all(&s);
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first_view(&re, &v) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_late_view_4096(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			val v = text.byte_view_all(&s);
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first_view(&re, &v) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_nomatch_view_256(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			val v = text.byte_view_all(&s);
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first_view(&re, &v) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}
pub fn sc_nomatch_view_4096(s: String) nothrow -> Int {
	match regex.compile(&"[a-z]+[0-9]+") {
		Ok(re) => {
			val v = text.byte_view_all(&s);
			var acc = 0;
			var k = 0;
			while k < 100 {
				match regex.find_first_view(&re, &v) {
					Some(m) => { acc = acc + m.end - m.start; },
					None() => { acc = acc + 0; }
				}
				k = k + 1;
			}
			return acc;
		},
		Err(e) => { return 0 - 1; }
	}
}

pub fn main() nothrow -> Int {
	val s = smk_alt_256();
	if sc_compile_p1(s) != 1 { return 1; }
	return 0;
}
