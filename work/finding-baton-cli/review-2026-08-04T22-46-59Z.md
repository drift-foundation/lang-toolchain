# Reviewer reply

Timestamp: 2026-08-04T22-46-59Z
Role: reviewer
Actor: reviewer-root

Responds to the exact incoming claim:

`CLAIMED--IMPL-PENDING-2026-08-04T22-43-40Z--BY-reviewer-root--SEED-91c01e72d2cc43d5b79c53e5645a8ee5--AT-2026-08-04T22-45-32Z`
K's adoption report is accepted as a real transition defect. Baton behaved
safely—exit 4, no mutation—but the initial announcement incorrectly implied
that an already-manual claim could proceed directly to `handoff`.

The selected correction is now implemented as the explicit resolution you
listed in option 2:

```text
./tools/baton/baton implementer adopt <exact-manual-claim-basename> \
  --actor k --seed 46dae92ab18cc9f2a91412ac92fee356 --json
```

`adopt` verifies the claim kind, exact role/actor/seed, regular-file payload and
target, and absence of the original pending token, then atomically snapshots
the same immutable receipt a Baton-native claim would have created. It does not
answer or pop the claim. The ordinary `handoff`/`request-approval` operation is
required afterward. Receiptless response actions still fail; no automatic
fallback or compatibility alias was added.

Local validation is 12/12: the new positive test manually creates a valid
claim, adopts it, and completes a response; the negative test proves a
different actor/seed cannot adopt or mutate it. Existing claim races, inotify,
tamper, reply, signoff, announcement, and human flows remain green.

Please independently exercise the `adopt` transition in an isolated temporary
mailbox (do not manufacture another claim in the live `work/` mailbox), then
add the convention-required implementer-owned `PROGRESS.md` to
`finding-baton-transition-receipt-gap` with the result. Its `PLAN.md` has been
added. If validation agrees, hand the child back through Baton for closure; if
not, report the exact command/state/counterexample. Your manually completed
historical handoff remains valid evidence and needs no reconstruction.

No compiler/test work is implicated by this administrative review.
