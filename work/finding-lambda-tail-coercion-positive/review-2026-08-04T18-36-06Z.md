# Review: hidden-lambda return boundary, revision 2

Reviewed handoff: `work/IMPL-PENDING-2026-08-04T18-34-17Z` →
`finding-lambda-tail-coercion-positive/PROGRESS.md`.

## Outcome

Revision 2 closes the substantive review. The compiler implementation is
approved for broader/full-suite verification.

- The immediate `HiddenLambdaSpec` dead-catch/dead-break IIFE now pins the
  third `_body_is_divergent` finalizer with a full compile/run test, and K
  confirmed the pre-fix failure at that exact finalizer.
- The interface MIR assertion now requires exactly one
  `ConstructIfaceValue` and pins `Speaker <- Dog` by its type IDs' names.
- The non-implementing negative now pins exactly one relevant diagnostic.
- History no longer collapses the block-tail routing loss and spec-return
  overwrite into a false “one root” claim.

No further production-code change is requested.

## Reviewer verification

The three changed boundaries pass independently:

```text
./.venv/bin/python3 -m pytest -q \
  lang/tests/driver/test_hidden_lambda_return_boundary.py::test_iife_divergent_dead_break_body_finalizes \
  lang/tests/driver/test_hidden_lambda_return_boundary.py::test_cb_block_tail_iface_coercion_reaches_hidden_mir \
  lang/tests/driver/test_hidden_lambda_return_boundary.py::test_cb_nonimplementing_tail_one_clean_diagnostic

3 passed in 50.56s
```

Together with the prior reviewer run, the reviewed boundary/trailing-match
set is green. K reports the complete mandatory battery at 140 passed.

## Non-blocking bookkeeping correction

`PROGRESS.md` says the boundary module is “now 13 tests.” It contains 12
`test_*` functions: the prior 11 plus the new IIFE. The 139 → 140 aggregate
delta also confirms one added test. Correct that count when recording the
full-suite result; no code re-review is needed for this typo.

## Next action

Proceed with the broader/full suite. If it fails in this finding's changed
surface, update `PROGRESS.md` and hand it back normally. If it passes, record
the result and complete the release-announcement duty.

The separate `APPROVAL-PENDING-2026-08-04T16-13-18Z` still governs only the
two proposed comment edits in the existing test file. Do not make them until
Slawomir explicitly approves them; they do not block starting the suite.
