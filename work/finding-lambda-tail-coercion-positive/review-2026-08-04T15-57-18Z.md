# Review handoff — hidden lambda return boundary (refreshed)

Timestamp: 2026-08-04T15:57:18Z

K: take `finding-lambda-tail-coercion-positive` next.

Read the whole folder fresh, with `PLAN.md` and
`EVIDENCE-REFRESH-2026-08-04.md` as the current-tree routing authority. The
older `FINDING.md` remains useful root-cause history; the implementer-owned
`PROGRESS.md` still describes the 2026-08-03 snapshot and must be reconciled by
you, not edited by the reviewer.

The priority decision is evidence-based: on current main, the interface-tail
boundary still fails, the stored value-match silently returns 0 instead of 5,
and the stored terminal-call form still reaches LLVM with `FnResult<Unknown>`
and a Python traceback. P1.3's saved CallInfo probes are green; the remaining
diagnostic/test gaps are lower priority.

Follow regression-first order. Install/adapt the four red boundaries before
production changes, confirm each fails for the recorded reason, then implement
one reconstruction helper and the spec-return authority correction. Do not add
a call-resolver body-inference path, copy side tables across hidden HIR
instances, or special-case interface construction in MIR lowering.

Important refresh from the old plan: terminal-`throws` tail calls are
statement-form semantic exits and must not be synthesized into `HReturn`.
Drive that exclusion from existing CallInfo/signature authority. The normalizer
must serve both hidden worklists and preserve statement-form match, Void, and
empty-body behavior.

Because 0.34.2 is already mainlined, this slice owns the 0.35.0 compiler minor
bump unless another queued slice establishes it first. ABI remains 22. Complete
the history and announcement duties in the refreshed plan.

You may create new test files. Before changing the existing
`test_stored_capturing_lambda_diagnostic.py` comment/source that explicitly
says the stored terminal form is blocked, post an `APPROVAL-PENDING-*` proposal
for Slawomir as required by the existing-test rule. That approval need not
block red-test creation or compiler work.

Long corpus verify/promotion is deferred across the queued findings. Run only
focused gates (and an optional quick corpus check if it does not stall the next
handoff), update `PROGRESS.md`, consume this exact review token, and publish one
work-level `IMPL-PENDING-*` token when ready for review.
