# Review: hidden-lambda return boundary, revision 1

Reviewed handoff: `work/IMPL-PENDING-2026-08-04T16-38-19Z` →
`finding-lambda-tail-coercion-positive/PROGRESS.md`.

## Outcome

The substantive compiler implementation is statically coherent. I found no
code defect in the shared hidden-body normalizer, concrete spec-return
authority, local interface-coercion fallback, or the new flow-authority-backed
MIR finalization. In particular:

- both hidden reconstruction worklists call the same normalizer before
  `normalize_hir` assigns the standalone function's node/callsite IDs;
- a genuine concrete block tail becomes a local `HReturn`, so the existing
  return authority records lowering-visible coercions on the independently
  checked hidden body rather than borrowing stale enclosing side tables;
- statement-form matches and recorded terminal calls remain statements;
- `LambdaFnSpec.return_type` is authoritative and body inference is now only
  the absent/Unknown fallback;
- `_body_is_divergent` delegates exit/effect semantics to `hir_flow` with the
  same CallInfo-backed predicates as the checker, and seals only a final open
  MIR block after the HIR authority proves no fallthrough;
- the named, hidden, and captureless call sites retain the genuine
  missing-return assertion when fallthrough remains possible.

No call-resolver body-inference path, enclosing-side-table copy, or
interface-specific MIR workaround was introduced. The 0.35.0 minor bump and
unchanged ABI 22 are correct.

This is not terminal signoff yet. One mandatory route pin and two promised
boundary assertions are missing; they are small and do not require a redesign.

## P1 — pin the third MIR-finalization route before broader gates

The adjacent `_body_is_divergent` change is installed at three production
finalizers:

1. ordinary/named `lower_function_body`;
2. `HiddenLambdaSpec` immediate/callback lowering;
3. captureless stored `LambdaFnSpec` lowering.

The new named-function test covers (1), and the existing stored divergent
lambda battery covers (3). I do not see a full compile/run test that forces
the new `ret_val is None` + open-block branch in (2). The existing immediate
terminal-call test terminates the builder directly, so it does not exercise
the new `_body_is_divergent` finalizer.

Add a driver case to the new boundary module using an immediate lambda with
the dead-catch/dead-break loop shape, for example an IIFE equivalent of the
stored case:

```drift
val a = try (|n: Int| -> Int => {
	while true {
		if n > 0 { throw ExcA(kind = 1); }
		try { continue; } catch { break; }
	}
})(1) catch { 10 };
```

Compile and run it, pinning the semantic exit. Confirm in the progress record
that the pre-fix branch failed at the hidden-lambda finalizer. This is the
Rule-3 companion for the second lowering-visible acceptance route, not a
request for another implementation authority.

## P2 — make the new boundary assertions match their stated contract

1. `test_cb_nonimplementing_tail_one_clean_diagnostic` promises exactly one
   clean diagnostic but only checks substring presence. Pin the relevant
   message count as exactly one; retain the traceback/SSA/contract exclusions.
2. The MIR test currently accepts any `M.ConstructIfaceValue` in the hidden
   function. Pin the selected instruction's `iface_ty` as `Speaker` and
   `value_ty` as `Dog`, as PLAN matrix item 1 requires. The current source is
   simple enough that the weaker assertion passes today, but the exact type
   pair is the checker→MIR contract under review.

## P2 — clean one overstrong history statement

The history heading says the three failure shapes have “one root.” The
interface/value-match failures share the missing block-tail-to-return routing;
the terminal-tail traceback additionally required fixing the independent
unconditional overwrite of the concrete `LambdaFnSpec` return. Describe them
as coupled hidden-return authority leaks (or otherwise avoid claiming one
root). Keep the adjacent divergent-finalization issue described separately.

## Separate human-approval gate

`APPROVAL-PENDING-2026-08-04T16-13-18Z` concerns only the two proposed stale
comment edits in the existing
`test_stored_capturing_lambda_diagnostic.py`. It did not block this code
review and must remain untouched until Slawomir gives an explicit ruling.

## Reviewer verification

Ran on the submitted tree:

```text
./.venv/bin/python3 -m pytest -q \
  lang/tests/driver/test_hidden_lambda_return_boundary.py \
  lang/tests/driver/test_lambda_trailing_match_value.py

14 passed in 328.81s
```

After the narrow changes above, rerun the new module plus the mandatory
focused battery. Do not start the full suite until the next review closes this
handoff. The release announcement remains a post-signoff action.
