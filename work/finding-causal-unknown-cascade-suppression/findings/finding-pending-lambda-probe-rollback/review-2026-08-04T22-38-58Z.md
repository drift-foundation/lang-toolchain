# Reviewer changes requested

Timestamp: 2026-08-04T22-38-58Z
Role: reviewer
Actor: reviewer-root

Responds to the exact incoming claim:

`CLAIMED--IMPL-PENDING-2026-08-04T22-23-12Z--BY-reviewer-root--SEED-91c01e72d2cc43d5b79c53e5645a8ee5--AT-2026-08-04T22-36-06Z`
The new evidence closes the requested research gaps sufficiently to select a
leading implementation design. Static inspection agrees with the probe:
captureless lambda typing publishes `LambdaFnSpec` entries at the two lambda
finalization sites, and each spec retains the live function-frame
`call_info_by_callsite_id` object. That registry is checker-global and outside
`CheckerStateTxn`, so the observed post-rollback entry is a real escaped
publication, not harmless TypeTable interning or a fingerprint artifact.

The mutation-site barrier is the better-supported Option A. It resolves exact
lexical binding identity on the live path, covers deferred producers by where
state is consumed rather than by a maintained syntax census, and avoids the
prewalk's unlinked/shadowed-HVar ambiguity. No current evidence forces the much
larger staged-resolution Option D.

Proceed with that design when the existing shared-edit gate is open, subject to
these implementation checks. They are acceptance constraints, not a prescribed
class layout; if implementation evidence contradicts the design, stop and
return the counterexample instead of forcing it through.

1. Make pending-lambda state an explicit owner, not a wrapper around only the
   two current `pop` sites. Registration, exact-id lookup/resolution, consume,
   and end-of-function drain must route through it. A direct mutation of the
   underlying map should be structurally unavailable outside the owner. This
   includes both current HCall/HInvoke consumers and the parent's eventual HVar
   value-use path.

2. The transaction check must happen before the pending HLambda is stamped,
   typed, removed, rebound, or published globally. Preserve lexical scope
   linking before the check where needed, because that mutation belongs to the
   probed subtree and supplies the authoritative binding id; do not replace it
   with `binding_id_by_name`.

3. Use a private structured barrier signal, distinct from diagnostic-based
   NEEDS_EXPECTED classification. At a nested probe, roll back that probe and
   re-raise. Only the outermost active probe rolls back and converts the signal
   to the ordinary silent expected-context deferral. It must not install
   `_defer_probe_hard_error`, emit a diagnostic, or increment
   `rollbacks_exception`. Add the proposed distinct barrier counter with
   documented semantics; count outermost deferrals separately from any nested
   rollback plumbing so corpus deltas remain interpretable.

4. The red-first regression must demonstrate the current failure and the green
   result using independent snapshots of the whole function HIR, unowned frame
   channels, owner fingerprint/allocators, and `_lambda_fn_specs`, including
   the live-map alias check. Do not weaken the assertion to final user output;
   this repro's output happens to remain correct despite the contract breach.

5. Pin nested transaction propagation even if no natural source shape is
   currently constructible. A focused transaction/owner unit test may open an
   outer and inner probe explicitly and exercise the barrier authority; it must
   prove inner rollback/re-raise and outer rollback/deferral without mutation.
   Also retain the COMPLETE control as compile-and-run coverage and verify its
   finalized CallInfo/type is unchanged after the expected-context retry.

6. Keep ordinary safe probes enabled, add direct HCall and HInvoke coverage,
   and include exact-id shadowing/unlinked-linking coverage. Rescan
   `doc/refactor_triggers.md` at implementation start. No spec or ABI change is
   indicated; keep this on the pending 0.35.0 train unless release state has
   changed.

Existing tests must not be edited without Slawomir's approval. New regression
files can carry the red/green matrix; if an existing transaction test is the
materially better home, request approval before editing it. Shared compiler
implementation remains gated exactly as before; this review does not infer
that the concurrent full-suite gate has cleared.
