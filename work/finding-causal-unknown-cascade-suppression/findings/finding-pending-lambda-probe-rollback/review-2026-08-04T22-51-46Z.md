# Reviewer announcement

Timestamp: 2026-08-04T22-51-46Z
Role: reviewer
Actor: reviewer-root
Slawomir reports the MEMCHECK suite completed successfully and explicitly moves
us to this next finding. The shared compiler-edit gate is now clear.

Planning is complete. Begin implementation of the selected mutation-site
pending-lambda transaction barrier, using the accepted constraints in
`review-2026-08-04T22-38-58Z.md` and the design checkpoint recorded in
`review-2026-08-04T22-48-05Z.md`.

At implementation start:

1. Re-read the current compiler tree and `doc/refactor_triggers.md`; report a
   newly matching trigger or stale premise before coding around it.
2. Establish the red-first state-identity regression before the fix. It must
   prove the current rollback leak across whole-body HIR, unowned frame state,
   owner fingerprint/allocators, and `_lambda_fn_specs`, including the live
   call-info-map alias.
3. Implement one explicit pending-lambda owner covering registration,
   exact-binding resolution/take, consume, and final drain. Keep direct backing
   map mutation structurally unavailable to consumers.
4. Raise the private barrier before any external lambda stamp/type/pop/rebind
   or global publication. Preserve live lexical binding linking; do not use the
   function-wide name map as shadowing authority.
5. Pin nested transaction propagation: inner rollback/re-raise, outermost-only
   silent deferral, no hard marker/diagnostic/exception counter, and distinct
   interpretable barrier counters.
6. Preserve the COMPLETE B5 control with finalized function type/CallInfo and
   compile-run outcome, plus HCall/HInvoke, exact-id shadowing/unlinked-linking,
   ordinary safe probes, and the eventual HVar value-use owner route.

Prefer a new regression file. Existing tests still require Slawomir's explicit
approval before editing; request it through Baton if an existing file is the
materially better home. No spec or ABI change is expected. Keep the work on the
pending 0.35.0 train unless current release state disproves that assumption.

The design is a falsifiable leading hypothesis, not an instruction to force a
particular class shape. If implementation reveals a bypass, nested-conversion
counterexample, or a second speculative-state escape family, stop and hand the
evidence back; Option D remains the escalation path.

Run focused gates only while implementation/review converges, record red/green
evidence and counter deltas in implementer-owned `PROGRESS.md`, and return the
actual diff through Baton for static review.
