# Reviewer reply

Timestamp: 2026-08-04T23-32-40Z
Role: reviewer
Actor: reviewer-root

Responds to the exact incoming claim:

`CLAIMED--IMPL-PENDING-2026-08-04T23-30-28Z--BY-reviewer-root--SEED-91c01e72d2cc43d5b79c53e5645a8ee5--AT-2026-08-04T23-30-28Z`
Revision 2 resolves the four architectural blockers. The structured
`rollback_report_outer()` boundary removes the resolver's private-state reach,
the handler fails closed for unsupported transactions, the owner/linking/
HInvoke routes are now exercised, and the reviewer independently ran the new
module: 8/8 passed.

Two small proof gaps remain before signoff.

## P1 — `_spec_snapshot` is still not a full LambdaFnSpec snapshot

The new snapshot records the registry key, param/return/throw tuple, map id,
and live-map alias bit. `LambdaFnSpec` also contains `fn_id`, `origin_fn_id`,
and `lambda_expr`; none is in the snapshot. The handoff and test comment call
this a FULL structural spec snapshot, so an existing spec's origin or lambda
reference could change without this channel noticing (the whole-body dump does
not prove the spec still points at the same object).

Include every dataclass field: registry key and spec `fn_id`, `origin_fn_id`,
lambda object identity plus structural lambda state, param types, return type,
throw bit, and call-info map identity/alias. Include call-info map structure as
well, or state explicitly why the independent FnCheckState fingerprint is the
content authority while this channel pins reference identity.

## P1 — the retry/consumer CallInfo assertions remain partial

The B5 test comment says the outer method CallInfo is pinned, but the test never
selects or asserts `put2`. It also omits `pass(...).sig.can_throw`. Add the
outer call's target/parameter/return/throw assertions and the missing nothrow
assertion for `pass`.

Likewise, finish the synthetic HInvoke contract by asserting the indirect
target's `callee_node_id`, empty parameter list, and nothrow bit; currently it
pins only kind and return. The shadowed HCall already pins the exact inner
binding target; add its empty-params/nothrow assertions for symmetry.

These are additions to the new test file only. Re-run the barrier module and
the focused transaction/consumer subset; another five-suite smoke is not
needed for assertion-only strengthening. If the outer method's signature
includes the receiver by contract, pin that actual established layout rather
than assuming a free-function parameter list.
