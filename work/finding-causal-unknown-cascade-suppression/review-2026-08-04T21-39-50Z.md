# Research handoff: causal Unknown cascade suppression

Timestamp: 2026-08-04T21:39:50Z

This is a preparation handoff, not authorization to edit compiler or test
files while the current `run_all_tests.sh` gate is active.

I selected this as the next substantive finding after the suite because it is
a confirmed diagnostic-state LANGUAGE_BUG. The remaining
`finding-true-statement-throwing-iife` looks like a smaller coverage audit of
already-landed routing and stays separate.

Please read the refreshed `FINDING.md`, `EVIDENCE.md`, `PLAN.md`, and existing
work-only probe in full. The documents were updated against the committed
pending-`0.35.0` tree without running tests. In particular, independently
check these points rather than accepting the proposed binding-cause table on
authority:

1. The two reported consumers still use a function-global
   `any(error in diagnostics)` predicate.
2. The separate `HInvoke` fallback may need policy parity even though parsed
   stored calls currently use `HCall(fn=HVar)`.
3. A bare value read of a pending stored lambda appears able to reach
   `_require_copy_value` before final flush emits its primary diagnostic.
4. A one-hop alias may require explicit cause propagation; exact binding
   identity alone could otherwise stop suppressing at the alias.
5. Any mutable cause state belongs in `FnCheckState.OWNED_TABLES` if expression
   typing can touch it, so transactions and fingerprints remain total.

For now, perform static preflight/research only. Record agreements,
counterexamples, a better authority if found, and proposed red regressions in
implementer-owned `PROGRESS.md`. Do not modify existing tests without
Slawomir's explicit approval, and do not modify compiler/new test files until
the current full suite has completed and Slawomir clears this slice to start.

Consume this review token after recording the preflight. If the research is
ready for reviewer follow-up, publish an `IMPL-PENDING-*` pointer to
`PROGRESS.md`; label it clearly as preflight-only so it cannot be mistaken for
an implementation handoff.
