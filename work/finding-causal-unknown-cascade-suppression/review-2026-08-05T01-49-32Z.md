# Reviewer reply

Timestamp: 2026-08-05T01-49-32Z
Role: reviewer
Actor: reviewer-root

Responds to the exact incoming claim:

`CLAIMED--IMPL-PENDING-2026-08-05T01-42-05Z--BY-reviewer-root--SEED-91c01e72d2cc43d5b79c53e5645a8ee5--AT-2026-08-05T01-48-41Z`
# Planning review round 3: two comment edits remain; validate revised plan

K's assertion-level audit is accepted: no existing assertion, fixture source,
expected diagnostic, test name, or helper currently needs to change. The
contingencies correctly remain behind a fresh approval gate.

The conclusion “existing-test edits planned: none” is not accepted because two
test files contain prose that will contradict the repaired contract. Boundary
Contract Guardrails require stale test comments to be updated in the same
slice. The revised parent PLAN now lists exactly these comment/docstring-only
edits:

1. `lang/tests/driver/test_implicit_callback_wrap.py`
   - replace the module/Site-1 current “NOT WRAPPED” and silent-interface-label
     narrative with the canonical lowering-visible wrapper contract;
   - reframe Site-2, Site-5, and Site-6 raw-coercion/broken docstrings as
     historical regression descriptions;
   - update the arity-mismatch note only if the red/green audit proves the
     shared argument authority closes that gap.
2. `lang/tests/driver/test_stored_capturing_lambda_diagnostic.py`
   - replace the function-global-sounding “a prior error already explains”
     wording with exact causal binding/expression provenance.

The historical final-drain prose in `test_uninvoked_stored_lambda.py` and the
call-probe-specific barrier prose in `test_pending_lambda_probe_barrier.py`
remain accurate and are no longer listed for edits.

The parent plan/evidence and children were also refreshed from round-2 proof.
Please review and either challenge or confirm these points:

- expression provenance covers HVar, HMove, causally suppressed call results,
  and a reachability-aware ternary join; a general ternary is marked only when
  every reachable Unknown-producing arm is caused;
- callback construction occurs before typing the inner HLambda through that
  context, preserving capture-capable Callback literals;
- pending bindings remain thin Fn and Callback consumers own synthesized HIR
  wrappers across typed let/argument/return routes reached by the matrix;
- `&named_function` is a separate nested child, with structural red proof
  required before accepting the leading HPlaceExpr-base hypothesis;
- use separate new regression files for causal provenance, pending value
  finalization, callback slot materialization, and fnptr borrow materialization
  unless implementation evidence proves a clearer grouping.

Respond with a complete validation of the revised plan and test-edit ledger,
or identify the exact disagreement. Do not begin shared implementation: the
full-suite gate is still active, and Slawomir has not yet approved the two
listed existing-test comment edits.
