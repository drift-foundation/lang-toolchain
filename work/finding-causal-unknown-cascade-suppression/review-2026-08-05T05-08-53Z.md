# Baton message

Timestamp: 2026-08-05T05-08-53Z
From role: reviewer
Actor: reviewer-root
To role: implementer
Kind: review
Thread: b49addf2e8d5

Responds to the exact incoming claim:

`CLAIMED-FROM-implementer-TO-reviewer-2026-08-05T05-04-41Z-8cc3938ed286-BY-reviewer-root-SEED-91c01e72d2cc43d5b79c53e5645a8ee5-AT-2026-08-05T05-04-41Z`
Revision 2 review: one P1 remains; the other round-two items are clear.

P1 — the associated/static Callback repair still loses a stored named-function value.

Reviewer repro: `work/finding-causal-unknown-cascade-suppression/probe_assoc_callback_fn_alias.drift`.

The source stores a named function and passes the binding: `val f = add1; return S::take_cb(f) - 7;`. Full compilation currently fails upstream of LLVM with:

`internal: MIR validation contract failure ... MoveOut of uninitialized iface local f`

Command used:

`./.venv/bin/python3 -m lang.driftc.driftc work/finding-causal-unknown-cascade-suppression/probe_assoc_callback_fn_alias.drift --entry probe_assoc_callback_fn_alias::main --target-word-bits 64 -o work/finding-causal-unknown-cascade-suppression/probe_assoc_callback_fn_alias.bin`

Static cause: the Site-1 post-resolution pass can recover a bare function NAME from `fnptr_consts_by_node_id`, but after `val f = add1` the argument HVar has already been silently labeled as the Callback interface and has no fnptr const on that argument node. `_s1_have_def` is therefore INTERFACE, `_s1_fp` is absent, and the pass `continue`s without wrapping. `_implicit_callback_wrap` can consume `fnptr_const_for_binding`, but the binding table currently records finalized pending lambdas and their immutable alias hops, not this stored named-function producer.

Please close the whole advertised fn-typed-argument contract, not only bare function names. One plausible route is to retain/propagate the static named-function provenance at the binding, but revalidate whether preserving the pre-coercion argument type/provenance before `_coerce_args_for_params` gives a cleaner single authority. Add a full compile/run positive for the stored alias; an immutable second alias is a useful propagation boundary. Any genuinely runtime-only fn value that v1 cannot wrap must receive a clean checker diagnostic rather than reaching a MIR invariant.

Clear in this pass:

- caused HCall callee suppression now observes independent non-lambda arguments; reviewer probe is green;
- caused method receiver suppression now occurs after ordinary argument observation; reviewer probe is green;
- HMatchExpr and HTryExpr caused joins have producer marks and conservative mixed-cause controls; reviewer probes are green;
- the fnptr borrow pin now actually calls through `(*r)()`.

Handoff bookkeeping: the implementation response said the approved Site-1 assertions were untouched, but K subsequently claimed the separate human-approval baton and the shared test file now contains those edits. That authorization is valid, but this review cannot sign off a moving test snapshot. The next implementation handoff should describe the final approved test changes and their focused results.
