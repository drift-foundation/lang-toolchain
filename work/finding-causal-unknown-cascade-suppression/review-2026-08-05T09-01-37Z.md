# Full-suite failure review: std.json legacy-helper fixture

Static and focused reviewer result: K's root-cause classification is confirmed. The compiler change should not be weakened to restore method-resolution cascades for a causally poisoned `Unknown` receiver.

## Observed

The current existing fixture obtains `arr` and `obj` from two rejected associated calls:

- `json.JsonNode::new_array()`
- `json.JsonNode::new_object()`

Those calls correctly remain primary diagnostics. Their bindings are therefore causally poisoned `Unknown` values. The later `array_push` and `object_set` messages previously came only from secondary method lookup on those poisoned receivers; withholding those two cascades is the intended contract of this finding.

Reviewer probe: `probe_std_json_legacy_primary.drift`. On the current tree it emits exactly four diagnostics when the four removed surfaces are made independent:

1. `E-QMEM-NO-CTOR` naming `new_array`;
2. `E-QMEM-NO-CTOR` naming `new_object`;
3. `no matching method 'array_push' for receiver std::std.json.JsonArray` after `var arr = json.new_array();`;
4. `no matching method 'object_set' for receiver std::std.json.JsonObject` after `var obj = json.new_object();`.

There is no `receiver Unknown` diagnostic. This preserves the migration test's real contract—every removed helper gets an actionable primary—without making cascade policy depend on the particular upstream cause category.

## Recommended existing-test edit (awaiting Slawomir approval)

Update only `lang/tests/driver/test_std_json_regressions.py::test_std_json_legacy_node_mutation_helpers_are_rejected`:

- retain independent calls to `json.JsonNode::new_array()` and `json.JsonNode::new_object()` for their associated-constructor primaries;
- construct valid method receivers with `json.new_array()` and `json.new_object()`;
- call the removed `array_push` and `object_set` names on those valid receivers, using independent `JsonNode` values so argument poisoning cannot become part of the contract;
- retain the four name-presence assertions and add an exact diagnostic-count pin of four plus a negative pin excluding `receiver Unknown`.

One fixture is sufficient; splitting into two driver invocations is optional rather than required by the evidence. K remains free to split it if focused evidence reveals an isolation benefit.

This file was outside the approved existing-test ledger, and changing its fixture source requires Slawomir's explicit approval. No test edit should occur until that approval is recorded.

## Verification and ownership after approval

K should run the affected driver file and the current focused causal-diagnostic battery, then report readiness through Baton. K should not restart `run_all_tests.sh`; under the repository's newly recorded execution-ownership rule, Slawomir owns the next full-suite run unless he explicitly delegates it.
