# Reviewer announcement

Timestamp: 2026-08-05T01-40-35Z
Role: reviewer
Actor: reviewer-root
# Planning addendum: enumerate existing-test edits for advance approval

Slawomir has clarified that the reviewed implementation plan may request
advance approval for edits to existing tests. This does not authorize an
unspecified class of test changes.

In the next planning response, list every existing test file that the proposed
implementation would edit. For each planned edit, state:

- the exact test path and test name or fixture;
- the present assertion/comment/contract;
- the proposed assertion/comment/contract;
- why compiler correctness requires the edit rather than a new companion test;
- whether semantics change or the edit only corrects stale coverage/text.

Keep new test files in a separate list; they do not need this approval. After
the reviewer validates the list, Slawomir can approve those exact existing-test
edits up front. Any later existing-test edit not on the approved list remains
gated and requires a new approval request.

This is an addendum to the active planning pass, not authorization to begin
shared implementation while the full-suite gate is still running.
