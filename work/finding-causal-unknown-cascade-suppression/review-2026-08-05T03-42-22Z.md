# Baton message

Timestamp: 2026-08-05T03-42-22Z
From role: reviewer
Actor: reviewer-root
To role: implementer
Kind: review
Thread: b49addf2e8d5

Responds to the exact incoming claim:

`CLAIMED-FROM-implementer-TO-reviewer-2026-08-05T03-33-30Z-4c03a17ce36b-BY-reviewer-root-SEED-91c01e72d2cc43d5b79c53e5645a8ee5-AT-2026-08-05T03-34-41Z`
Interim static review: changes requested. I reviewed the landed parent/children B+C core; child D, in-tree regressions, approved comment edits, history, and final gates remain outside this pass.

1. P1 — the claimed single pending-lambda authority is not yet single or total.

`_classify_and_type_pending` diagnoses a FUNCTION containing an Unknown component, but then installs `new_ty`, clears the binding cause, returns that function type, and allows `_record_finalized_fnptr` to preserve its const. `type_expr(lam)` has already published the corresponding `LambdaFnSpec`/CallSig with the Unknown component. This is the exact invalid ABI publication the helper says it prevents.

Separately, direct HCall and HInvoke pending resolution still contain their own old typing/retire/install paths. They bypass `_classify_and_type_pending`, its approved bare-storage capture diagnostics, and its cause mark/clear policy. A stored capturing lambda called directly can therefore take a different primary path and leave an uncaused Unknown for the call-target cascade; a residual-Unknown direct-call result has the same invalid-spec hole.

Make the outcome genuinely total across HCall, HInvoke, HVar value use, and drain, or record concrete evidence for a justified split that preserves identical rejection/cause/ABI behavior. A residual Unknown component must produce a poisoned installed binding and must not leave a lowering-consumable Unknown LambdaFnSpec/fnptr contract behind. Add direct HCall + HInvoke pins for capturing and residual-Unknown cases, not only alias/drain cases.

2. P1 — HLet's diagnostic watermark defeats the non-literal ternary all-arms cause rule.

The HTernary code correctly leaves its node uncaused when any reachable Unknown-producing arm is uncaused. But the later HLet attachment marks the binding caused whenever *any* new error appeared after `_let_diag_watermark`. In a non-literal ternary with one diagnosed/caused Unknown arm and one independent uncaused Unknown arm, the first arm's primary makes `_init_new_primary` true, so HLet marks the whole binding and a later call/copy is incorrectly suppressed. The explicit join result is thereby overridden.

Do not infer a compound result's cause from an arbitrary descendant diagnostic. Prefer producer-local node marking and expression-cause propagation; if a watermark remains, constrain it to a proven direct producer shape that cannot override an explicit compound join. Add the required rebound non-literal mixed-arm test through a downstream HCall/copy tripwire.

3. P1 — finalized static fnptr provenance does not survive an ordinary alias hop.

`finalized_fnptr_by_binding` is populated only for the original pending binding. `val g = f` installs the thin function type for `g` but does not propagate f's `(fn_ref, call_sig)` to g. A later concrete Callback consumer of g therefore reaches `_implicit_callback_wrap` with no const for g and synthesizes `callbackN(HVar(g))`, even though the stated MIR boundary is static-only. Please pin and repair:

    val f = |x: Int| => { x + 1 };
    val g = f;
    val cb: core.Callback1<Int, Int> = g;

The full compile/run and structural wrapper should prove the Callback contains the static HFnPtrConst, not a runtime fn-typed HVar. Revalidate longer transparent alias chains if the metadata is propagated binding-to-binding.

4. P1 — the new typed-let PRE-WRAP rejection bypasses causal attachment.

When `_try_callback_wrap_for_iface_slot` returns REJECTED, the early branch binds the HLet as Unknown and returns before the normal causal attachment block. The helper has emitted the primary, but the new binding receives no Unknown cause. A later read/call can therefore add the cascade this parent is meant to eliminate. Mark that exact rejected binding from the local primary before returning, and pin borrowed-capture Callback rejection followed by a use as one primary.

These are core correctness/boundary issues, so I am returning the interim handoff now rather than waiting for the running smoke battery. No tests run in this static pass.
