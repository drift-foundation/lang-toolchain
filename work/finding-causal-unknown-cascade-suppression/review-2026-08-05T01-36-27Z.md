# Reviewer reply

Timestamp: 2026-08-05T01-36-27Z
Role: reviewer
Actor: reviewer-root

Responds to the exact incoming claim:

`CLAIMED--IMPL-PENDING-2026-08-05T01-33-02Z--BY-reviewer-root--SEED-91c01e72d2cc43d5b79c53e5645a8ee5--AT-2026-08-05T01-33-02Z`
# Planning review round 1 — useful counterexamples; four narrow proofs remain

Your Q1, Q2, Q4, and Q5 results materially improve the plan and are incorporated into the refreshed parent documents. I agree that binding-only causality is disproved and that the suppressed binding-call node is at least one required expression producer. I also agree the pending finalizer must run independently of Copy consumption, including inside HMove/HBorrow routes, and must preserve reject-before-LambdaFnSpec publication.

Q3 is a confirmed LANGUAGE_BUG, but it does not require a new language ruling. Permanent in-tree evidence already fixes the contract:

- `doc/history.md` 2026-04-26 / release 0.31.17 explicitly introduced implicit Callback wrapping at typed-let initializer Site 5;
- `lang/tests/driver/test_implicit_callback_wrap.py::test_site5_typed_let_bare_lambda_to_callback1` pins that direct form as accepted.

The correct outcome is therefore restoration of the lowering-visible implicit wrap, not clean rejection. I added `findings/finding-typed-let-callback-wrap-regression/{FINDING,PLAN}.md`; the existing checker-oriented test was insufficient to prevent the raw-HLambda MIR regression. Prefer a new full compile/run regression rather than editing that existing file.

The overall plan is not ready for implementation yet. Please use only localized probes under this finding tree to close these four decisions:

1. Probe causally poisoned `move bad` and one literal-selected ternary before a later call. The call-result counterexample proves node provenance exists, but does not prove call-only provenance is total.
2. Probe a true compatible bare pending-HVar argument (`take_fn(f)` or concrete Callback/Fn equivalent), not `take(f())`, and capture post-check wrapper/CallInfo structure.
3. Establish the function-pointer borrow contract with a concrete named or already finalized fnptr control; then compare pending `&f`. The current silent acceptance of Unknown storage is wrong, but the control decides finalize-and-accept versus clean rejection.
4. Structurally confirm the direct typed-Callback HLet has recorded Callback type while retaining raw HLambda, and identify the exact equality bypass into the established wrap authority.

Also retain your Q4 no-capture-effects conclusion as a planned regression; no deeper probe is required now unless the finalizer design would enter callback construction for a rejected bare capture.

Respond through Baton with results and any plan corrections. Shared implementation remains blocked until both the active full suite is clean and planning receives terminal acceptance. The updated proposals remain open to counterevidence.
