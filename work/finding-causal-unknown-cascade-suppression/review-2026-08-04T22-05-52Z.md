# Review: preflight transaction addendum — evidence accepted, architecture remains open

Timestamp: 2026-08-04T22-05-52Z
Author: reviewer-root

Responds to the exact incoming claim:

`CLAIMED--IMPL-PENDING-2026-08-04T21-54-35Z--BY-reviewer-root--SEED-91c01e72d2cc43d5b79c53e5645a8ee5--AT-2026-08-04T21-59-56Z`

The static transaction-boundary finding in
`preflight-addendum-response-2026-08-04T21-56-00Z.md` is accepted: an
allowlisted candidate can semantically reach a pending lambda outside the
candidate subtree, while pending resolution mutates state not covered by the
current transaction. The clean `id(f())` observation proves live probe reach,
not rollback leakage; a forcing rollback remains necessary before the finding
or any fix is considered proven.

Do not treat the earlier HLet-only sequencing or the reviewer's original
pending-map gate as an approved implementation. Slawomir has explicitly asked
us to investigate stronger long-term alternatives when a minimal fix would be
brittle. I expanded the provisional child finding at:

`findings/finding-pending-lambda-probe-rollback/`

Use its `FINDING.md`, `EVIDENCE.md`, `PLAN.md`, and new `DESIGN.md` to validate
the mutation inventory and compare the options. In particular,
pending-lambda typing appears able to mutate
external HLambda/capture/parameter nodes, binding metadata and allocators,
ordinary recorder tables, and checker-global `_lambda_fn_specs`; merely adding
`binding_types` and `pending_lambda_by_binding` to `FnCheckState` would not
establish a complete transaction.

The next useful evidence is a work-folder-only forcing probe that resolves a
pending lambda inside an enclosing deferred candidate and then forces that
enclosing probe to `NEEDS_EXPECTED` rollback. Extend the independent audit to
include whole-function HIR, frame containers, owner state, and relevant
checker-global registries. If the hypothesized rollback cannot be forced, or
the observed mutation closure contradicts the child, record that and reject or
revise the finding rather than fitting the probe to the review.

Compare at least these directions after the evidence exists:

1. a centralized semantic transaction-purity/barrier authority;
2. complete owner-by-construction plus dependency-closed HIR/global rollback;
3. non-speculative pending-dependency stabilization;
4. staged/pure resolution plans applied only on commit;
5. persistent lambda inference variables eliminating pending mutation.

A semantic gate may still be the best engineering choice, but only if it is a
complete, exact-binding, fail-closed authority for all deferred semantic
dependencies—not a one-off lookup that future callable forms bypass. K may
prefer another design or identify a missing alternative; document the evidence
and reasoning either way.

Current scope remains research-only while the full suite runs: isolated probes
inside the finding may invoke the compiler; do not edit shared compiler, tests,
spec, history, fixtures, or infrastructure yet. No spec change is authorized.
