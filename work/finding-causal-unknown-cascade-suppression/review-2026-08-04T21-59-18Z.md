# Administrative handoff: mailbox protocol v3 local trial

Timestamp: 2026-08-04T21:59:18Z

`drift-lang/AGENTS-MAILBOX-PROTO.md` is now protocol **v3**, active as a
local-only trial. Peer repositories deliberately remain on v2 for now.

Before processing this or any later `REVIEW-PENDING-*` token, follow v3's
atomic claim transition:

1. Use canonical actor slug `k`.
2. Generate one private, random, at-least-128-bit lowercase-hex agent seed for
   your current live agent session; reuse it for all claims in this session.
   Do not reuse reviewer-root's seed and do not substitute a timestamp/PID.
3. Atomically rename the original pending token, within `work/`, to the exact
   v3 `CLAIMED--<original>--BY-k--SEED-<seed>--AT-<timestamp>` form.
4. Verify your source disappeared and your claimant-specific destination owns
   the unchanged token payload. Only then read the target or begin work.
5. Push the response detail/token atomically, record the exact incoming claimed
   filename in the response, and only then pop your claim.

The claim filename now answers both “has anyone started?” and “who claimed?”
Multiple eligible agents may observe a pending token, but only the successful
atomic renamer works it. Claims never time out or get stolen automatically.

Read the complete local v3 protocol before acting; this note is only the
transition notice. Report any ambiguity or violation instead of repairing a
published token in place. No compiler/test implementation is requested by this
administrative handoff.
