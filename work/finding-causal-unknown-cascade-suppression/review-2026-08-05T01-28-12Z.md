# Reviewer announcement

Timestamp: 2026-08-05T01-28-12Z
Role: reviewer
Actor: reviewer-root
# Planning review request — refreshed causal provenance slice

Please independently review the refreshed parent material:

- `FINDING.md`
- `EVIDENCE.md`
- `PLAN.md`
- `findings/finding-pending-lambda-value-finalization/FINDING.md`
- `findings/finding-pending-lambda-value-finalization/PLAN.md`

The leading, explicitly falsifiable proposal is to fold the confirmed captureless pending-value finalization bug into the causal Unknown-provenance slice. The just-landed `PendingLambdaOwner` barrier changes the design boundary: one exact-binding finalizer may now serve HCall, HInvoke, ordinary HVar references, and final drain without reopening the probe-rollback leak, while one transaction-owned cause authority replaces the two function-global suppression scans and HInvoke's opposite policy.

Please challenge that premise before implementation. In particular, determine from localized red probes whether:

1. binding-only cause state is total or poisoned call results require node/expression provenance;
2. one pending HVar hook covers aliases, returns, compatible arguments, move/borrow/discarded reads, and deferred-probe retries without changing ownership or lowering contracts;
3. contextual Callback aliasing retains a thin function-pointer binding and records a real lowering-visible wrapper;
4. bare capturing finalization emits the approved storage primary without applying invalid capture effects;
5. unconstrained finalization rejects before publishing any `LambdaFnSpec` with Unknown ABI types.

While `run_all_tests.sh` is active, Slawomir authorizes you to create and run narrow compiler-invoking probes only inside this finding tree. Do not edit shared compiler, repository-test, fixture, history, spec, or infrastructure files, and avoid materially competing with the suite.

Respond through Baton with your review, corrections, counterexamples, and proposed ready-plan changes. Shared implementation remains blocked until the full suite is clean and reviewer/implementer planning has converged. No proposal in the refreshed files is authoritative over contrary code or probe evidence.
