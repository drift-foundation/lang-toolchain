# Reviewer announcement

Timestamp: 2026-08-05T02-44-49Z
Role: reviewer
Actor: reviewer-root
# Implementation start authorized

At 2026-08-05T02-44-19Z, Slawomir reported that he intentionally killed the
active `run_all_tests.sh` run after observing it remain clean and explicitly
directed the team to proceed with work. Record this as a human-cleared start
gate, not as a terminal full-suite pass; the interrupted log has no final
summary. The full scope is deliberately deferred until the last queued finding
is fixed, when one final full-suite run will validate the accumulated train.

The other gates are already clear:

- reviewer and implementer accepted the revised parent/child plan;
- Slawomir approved only the exact two-file existing-test comment/docstring
  ledger recorded in `review-2026-08-05T02-24-41Z.md`;
- no specification or ABI change is planned.

K is authorized to begin the shared implementation in
`finding-causal-unknown-cascade-suppression` and its three active children,
using the accepted red-first plan. Preserve these specific review constraints:

1. cause provenance is exact binding + expression-node state owned by
   `FnCheckState`; ternary propagation is reachability-aware and fails toward
   the tripwire if any reachable Unknown-producing arm is uncaused;
2. every pending-lambda resolution route enters
   `PendingLambdaOwner.begin_resolution()` before mutation, publishes no
   Unknown ABI spec, and stores a thin Fn—not Callback—on accepted bindings;
3. Callback consumers construct and splice the canonical wrapper before the
   inner HLambda is typed through Callback context; no interface-label-only
   acceptance;
4. the named-function borrow mechanism remains a hypothesis until a structural
   red test traces the HPlaceExpr/HFnPtrConst transition;
5. use four separate new regression files unless evidence demonstrates a
   clearer boundary; every lowering-visible acceptance has a compile/run pin;
6. only the two approved existing-test comment/docstring edits may be made.
   Any assertion, fixture, expected-output, helper/name, deletion/rename,
   contingency, or other existing-test edit requires a fresh approval gate.

Re-scan `doc/refactor_triggers.md` before production changes, keep the current
spec intact, and hand back focused evidence through Baton. If red evidence
contradicts the plan, stop that path and report the disagreement rather than
forcing the proposed design.

Do not clean up, delete, or archive this finding after the current slice. Keep
its parent/child research, reviews, and evidence in place until the deferred
final full-scope run and subsequent cleanup decision.
