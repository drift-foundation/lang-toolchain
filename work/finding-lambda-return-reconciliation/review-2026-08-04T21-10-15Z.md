# Review: return reconciliation + folded P1.3, revision 2

Date: 2026-08-04T21:10:15Z

Outcome: **signed off for broader gates**.  No further implementation change
is requested.

All revision-1 findings are closed:

- The nested stored-lambda intrinsic CallInfo LANGUAGE_BUG is fixed at
  finalized `TypedFn` side-table ownership and separately signed off in the
  child finding.  The validator remains strict.
- The primary reconciliation test now pins the exact stable diagnostic code,
  exact message, and offending `HReturn` span.
- The new test-module prose and permanent history correctly distinguish the
  contextual declared-return rejection from the genuinely uncontextual case
  formerly caught only by the hidden re-check.

The original reconciliation and folded P1.3 conclusions stand: return values
are observed from their single authoritative typing visit, nested collectors
are isolated, all observations are reconciled without late coercion or
retyping, `_find_return_expr` is gone, and the unreachable duplicate
`HCall(fn=HLambda)` resolver branch is deleted.  The approved existing-test
edits remain comments only.

Independent reviewer verification across the two passes:

```text
lang/tests/type_checker/test_inferred_lambda_return_reconciliation.py
lang/tests/type_checker/test_lambda_callinfo_inference_boundary.py
18 passed (revision 1)

lang/tests/driver/test_nested_lambda_callinfo_ownership.py
lang/tests/type_checker/test_inferred_lambda_return_reconciliation.py::test_mismatch_diagnostic_code_and_span_are_stable
5 passed in 100.68s (revision 2)

git diff --check
clean
```

K's combined focused/smoke gate may finish, followed by the repository's
broader gate sequence.  Any related failure reopens the appropriate parent or
child via a new mailbox handoff.  Pending unreleased version remains 0.35.0;
ABI remains 22.
