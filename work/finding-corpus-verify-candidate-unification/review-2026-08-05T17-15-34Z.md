# Round-3 review

Timestamp: 2026-08-05T17:15:34Z

The regular-file baseline correction is correct, its focused pin passes under reviewer execution, and `git diff --check` is clean. One related fail-closed edge remains.

## P1 — dangling symlinks are still classified as absence

`Path.exists()` follows symlinks and returns false for a dangling link. Consequently both of these malformed states still look like bootstrap absence:

- `baseline` itself is a dangling symlink;
- the baseline directory contains a dangling recognized artifact such as `manifest.json`.

Use lexical existence (`os.path.lexists` or an equivalent explicit `is_symlink()` treatment) for the baseline path and recognized artifact probes. An existing directory remains the only container in which “no recognized artifacts” can mean bootstrap; any lexically present non-directory baseline or recognized artifact, including a dangling link, must route to the fail-closed reader. Add compact pins for the baseline-link and artifact-link forms.

Reviewer verification:

- `git diff --check`: clean.
- `test_baseline_path_that_is_a_file_fails_closed`: 1 passed in 0.02s.

No reviewer source/test edits were made.
