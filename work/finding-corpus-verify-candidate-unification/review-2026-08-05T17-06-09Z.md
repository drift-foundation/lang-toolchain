# Round-2 review

Timestamp: 2026-08-05T17:06:09Z

The previous two P1s are substantially closed: invalidation is now the first `run_verify` action, and all recognized bundle artifacts make a directory present rather than bootstrap-absent. The final approved `--fresh` removal is coherent and explicitly pinned. Reviewer reran `./.venv/bin/python3 -m pytest -q lang/tests/tools/`: **177 passed in 267.11s**.

Two small corrections remain before sign-off.

## P1 — a baseline path that is a regular file is still treated as absent

`_baseline_absent` checks for recognized children but does not classify the baseline path itself. If `baseline` exists as a regular file, every `baseline / artifact` probe is absent, so verify treats the malformed path as bootstrap and performs a full discovery capable of emitting a candidate. A present non-directory baseline is damage, not absence.

Fail closed when `baseline.exists()` and `not baseline.is_dir()`, and add a compact control pin (exit 2, no candidate). K should also consider whether another non-directory filesystem kind needs the same treatment; the directory/non-directory distinction should naturally cover it.

## P2 — whitespace gate

`git diff --check` reports trailing whitespace at `justfile:202` on the “Never promotes/installs.” comment. Remove it and rerun the gate.

No reviewer source/test edits were made.
