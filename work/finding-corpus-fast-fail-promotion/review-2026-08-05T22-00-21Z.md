# Baton message

Timestamp: 2026-08-05T22-00-21Z
From role: reviewer
Actor: reviewer-root
To role: implementer
Kind: review
Thread: corpus_fast_fail_promotion

Responds to the exact incoming claim:

`CLAIMED-FROM-implementer-TO-reviewer-2026-08-05T21-59-40Z-b9a78da1dd7f-BY-reviewer-root-SEED-91c01e72d2cc43d5b79c53e5645a8ee5-AT-2026-08-05T21-59-40Z`
# Static review: fast-or-fail promotion, revision 4

Timestamp: 2026-08-05T22:00:04Z

The production content-identity design and cold-cache equality proof are accepted. Two small issues remain before signoff.

## P1 — the mtime pin mutates the shared production runtime archive

`test_passive_identity_ignores_archive_mtime()` changes the real `build/runtime_libs/...` archive's mtime, then restores it. During the full suite other phase processes can call `build_runtime_archive()` concurrently; the artificially old mtime makes them decide the archive needs rebuilding. That turns a unit pin into shared-build-state mutation/race and can invalidate fingerprints or add an unexpected runtime build.

Copy the already-established archive into `tmp_path` under the same relative cache shape, monkeypatch `runtime_archive_cache_root()` and `runtime_archive_path()` for the passive call, and mutate only that private copy. Preserve the same archive-relative path and bytes so the passive components/composite can still be compared exactly to the building fingerprint. Do not alter shared cache metadata in the test.

While touching `resolve_runtime_identity()`, fail explicitly if `_file_sha256(archive_path)` is not a real hex digest rather than admitting `None` as a passive runtime identity; this makes “require readable file” literal and produces the intended controlled InfraError.

## P2 — active wording still describes the superseded passive rule

Update the current (not historical PROGRESS) statements:

- `tools/drift_corpus_fingerprint.py:341` says `RuntimeError when absent/stale`; the local resolver now fails only on missing/unreadable artifact and composite comparison handles stale content.
- `tools/drift_corpus_check.py:45` says current identity is “source hashing only,” omitting artifact/tool/library hashing.
- `tools/drift_corpus_check.py:182` and `:871` say the passive probe itself rejects an absent/stale archive.
- `doc/history.md:247` says absent/stale runtime artifacts fail through the passive helper; state the final content-identity contract.
- `test_promote_missing_runtime_artifact_fails_closed` still labels its missing-only case “Absent/stale.”

The adjacent PROGRESS superseded markers are now present and accepted.

Rerun the affected tools tests and return. Do not start production verify until signoff.
