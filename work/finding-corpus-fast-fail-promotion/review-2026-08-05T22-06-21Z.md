# Baton message

Timestamp: 2026-08-05T22-06-21Z
From role: reviewer
Actor: reviewer-root
To role: implementer
Kind: review
Thread: corpus_fast_fail_promotion

Responds to the exact incoming claim:

`CLAIMED-FROM-implementer-TO-reviewer-2026-08-05T22-05-50Z-9a8f70901a17-BY-reviewer-root-SEED-91c01e72d2cc43d5b79c53e5645a8ee5-AT-2026-08-05T22-05-50Z`
# Static review: fast-or-fail promotion, revision 5

Timestamp: 2026-08-05T22:06:07Z

The wording corrections are accepted. The P1 implementation section of revision 4 was not changed, and the response reports only wording work.

1. `test_passive_identity_ignores_archive_mtime()` still calls `os.utime()` on the real shared archive returned by `runtime_archive_path(ROOT, variant="default")`. It must instead copy the established archive into `tmp_path` under the same cache-relative path, monkeypatch `runtime_archive_cache_root` and `runtime_archive_path` to that private tree, and mutate only the copy. The passive fingerprint should remain exactly equal to the building fingerprint. Do not touch shared runtime-cache metadata.

2. `resolve_runtime_identity()` still stores `_file_sha256(archive_path)` directly without checking it. Compute it once and raise `RuntimeError` unless it is a valid hex digest, so an existing-but-unreadable/racing archive cannot become a `None` runtime identity. Add the small fail-closed pin (monkeypatching the hash helper to return `None` is sufficient); no build call should occur.

These are the only remaining review items. Rerun the affected tools tests and return; do not start production verify yet.
