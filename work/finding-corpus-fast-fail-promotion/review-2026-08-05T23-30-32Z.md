# Baton message

Timestamp: 2026-08-05T23-30-32Z
From role: reviewer
Actor: reviewer-root
To role: implementer
Kind: corpus_review
Thread: corpus_fast_fail_promotion
# Fresh ownership-corpus candidate review

Timestamp: 2026-08-05T23:30:14Z

Slawomir's production `just ownership-corpus-verify` finished and published the schema-2 candidate at `build/tmp/ownership-corpus-projection.json`. Please independently review it and reply whether fast promotion is safe; do not modify source or promote.

Reviewer evidence:

- candidate validation: clean (`_validate_handoff -> None`);
- verify metadata: 1925.9 seconds, 16 jobs;
- exhaustive observation: 1,338 observed, 0 projected; 969 compiled_ok, 369 failed;
- hard gates: zero;
- candidate universe JSON is exactly equal to the installed reviewed baseline universe;
- candidate aggregate counters are exactly equal to the baseline aggregate counters;
- candidate per-fixture projections are exactly equal to baseline projections;
- candidate snapshot/toolchain differs from the installed baseline fingerprint, as expected after the corpus-tool contract changes; this should be an identity/provenance refresh with zero ownership-semantic drift.

One environment nuance: a passive recomputation inside Codex's sandbox initially mismatched only because Codex injects extra PATH entries. Recomputing after setting PATH to the candidate's recorded ordinary-shell PATH matches both candidate toolchain and run-snapshot composites exactly. Slawomir should run promote from the same ordinary shell environment that ran verify; please independently confirm this interpretation.

Expected next action after both reviews agree: Slawomir runs `just ownership-corpus-promote`; it should complete in seconds with zero compilation/builds. Then review the installed baseline diff before commit.
