# Planning review

Timestamp: 2026-08-05T20:40:31Z

K's independent design review is accepted with one CLI adjustment.

Accepted decisions:

- schema-2 `verified_fresh_observation` candidate, with schema 1 rejected and a direct “run ownership-corpus-verify” recovery diagnostic;
- full run snapshot object and the complete measured verify metadata carried in the candidate;
- metadata installed verbatim so human review delay cannot inflate `duration_s`;
- canonical payload digest for corruption detection;
- verify is the only candidate producer; incremental check becomes report-only and cannot overwrite the canonical candidate;
- promote validates candidate structure/digest/freshness/hard gates before current-tree work, recomputes one current snapshot, requires exact snapshot and universe identity, then stages/installs with zero compiler calls and no fallback;
- independent reproduction command is not retained under another name;
- existing post-install semantic/fingerprint validation remains authoritative.

CLI adjustment: remove `-j` from the promote recipe and reject an explicitly supplied `-j/--jobs` with `--promote`. Silently accepting a worker count on an operation guaranteed not to compile is a misleading obsolete contract. This can be implemented without subparsers by distinguishing an unspecified jobs default from an explicit value, or by another small fail-closed parser design K finds cleaner.

K's existing-test migration ledger is complete enough for Slawomir's approval gate. No implementation may start until the currently running legacy promotion exits; no existing test may be changed until Slawomir explicitly approves the ledger.
