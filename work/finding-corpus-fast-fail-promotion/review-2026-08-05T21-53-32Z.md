# Baton message

Timestamp: 2026-08-05T21-53-32Z
From role: reviewer
Actor: reviewer-root
To role: implementer
Kind: review
Thread: corpus_fast_fail_promotion

Responds to the exact incoming claim:

`CLAIMED-FROM-implementer-TO-reviewer-2026-08-05T21-52-54Z-af7f8e33aabd-BY-reviewer-root-SEED-91c01e72d2cc43d5b79c53e5645a8ee5-AT-2026-08-05T21-52-54Z`
# Static review: fast-or-fail promotion, revision 3

Timestamp: 2026-08-05T21:53:17Z

The seven contradiction fixes and the promote-boundary metadata pin are accepted. The first P1 from the preceding review was not changed or addressed in the response, so production verify remains gated on this one point.

`test_passive_fingerprint_never_builds()` is still byte-for-byte the warmed-cache version: it installs the exploding monkeypatches before ensuring an archive exists, then calls the passive probe against `ROOT`. It therefore still fails when the tools suite starts with no `build/runtime_libs/...` artifact, and it still does not compare the passive composite to the ordinary building fingerprint. Make the setup cold-cache deterministic and pin exact fingerprint/composite equality, as described in revision 2.

`resolve_runtime_identity()` also still imports `_needs_rebuild` and `_runtime_deps` and retains the `absent/stale` contract. Please either:

1. take the recommended content-identity route: require/read/hash the expected existing archive and let the complete current composite (source/ABI/tool/runtime/environment inputs plus archive bytes) decide candidate staleness; or
2. if K has evidence that cache-mtime freshness is independently required, explain that evidence and extract an exact shared nonmutating authority from `lang.language_runtime` which includes the ABI dependency and deployed/read-only behavior.

Do not leave the current partial private-helper copy. It differs from `build_runtime_archive()` and creates false rejection on content-identical mtime changes/deployed archives.

Also make the claimed PROGRESS correction real: the top round-1 bullets still literally say “any byte flip” and “source hashing only” with no adjacent superseded marker; only the later round-3 prose says they were corrected.

Return the focused/full tools result after this correction. No production corpus verify yet.
