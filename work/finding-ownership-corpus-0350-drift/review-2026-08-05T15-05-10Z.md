# Final review: ownership-corpus 0.35.0 drift

Timestamp: 2026-08-05T15:05:10Z

Disposition: **reviewer concurs with implementer; approve promotion unchanged**.

K corrected the initial reviewer hypothesis and supplied the missing named-function proof. I independently inspected her retained verbose-audit and LLVM evidence and re-extracted the inventories. The only semantic corpus delta is removal of this function:

```text
std.core::std.core.arc.Arc<T>::Borrow<T>::borrow__inst__703503d992a3c867
```

The transition is exact at reviewed commit `0c9413dd`: baseline and `f150cbce` have 1286 identical verbose function records; `0c9413dd` and current have 1285 identical records. Two current compiles are byte-identical. The removed baseline monomorph has `events: 0`, is defined once in baseline LLVM, is never called, and is wholly absent from current LLVM. Baseline and current fixture binaries both run with exit 0.

Static inspection agrees with that result. `0c9413dd` partitions callsite-indexed instantiations by ownership of the finalized parent body. A callsite from an extracted lambda therefore no longer queues a phantom parent-side generic instantiation; the extracted hidden body is independently checked and queues any instantiation it actually owns. The associated regression matrix tests both sides of that boundary and full compile/run behavior.

The remaining corpus bundle changes are expected identity or source-text changes:

- `DRIFTC_VERSION` 0.34.2 -> pending 0.35.0 and corresponding toolchain/run fingerprints;
- timestamps and duration;
- the already-reviewed comment-only source hash change in `autoborrow_owned_rvalue_field_method_drops_once`.

Fixture universe, inclusion rule, compiled/failed/excluded partitions, all ownership event/site counters, and every other fixture projection are unchanged. I find no ownership-semantic regression and agree with K's recommendation to promote the baseline unchanged from the fresh candidate.

Durable command-level evidence is recorded in `evidence/named-function-delta-2026-08-05T15-05-10Z.txt`.

## Promotion mechanics

No `build/tmp/ownership-corpus-projection.json` handoff currently exists. Under the repository's three-state corpus protocol, promotion therefore still requires Slawomir to create a fresh developer candidate and then promote it:

```text
just ownership-corpus-check --fresh
just ownership-corpus-promote
```

The second command performs its own fresh full compile and must exactly reproduce the candidate before it installs the reviewed baseline. Neither command has been run by the reviewer or implementer as part of this disposition.
