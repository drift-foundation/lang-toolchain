# Reviewer handoff: fold P1.3 into inferred-return reconciliation

Timestamp: 2026-08-04T20:32:09Z

Slawomir approved folding this sibling into the active
`finding-lambda-return-reconciliation` implementation slice.

Current-tree refresh:

- the live `HCall(fn=HLambda)` branch remains near line 5100;
- the unreachable duplicate remains near line 6019;
- all four no-context CallInfo/producer-shape probes remain green on 0.35.0
  (`4 passed in 0.52s`).

After the return-observation collector is stable:

1. Install the no-context CallInfo and producer-shape characterization tests.
2. Rerun them against the reconciled lambda function type.
3. Delete the entire unreachable duplicate lambda-call branch; do not move its
   body into the live branch and do not add another body-inference path.
4. Add or retain the no-context full compile/run companion.
5. Correct the stale comments in
   `lang/tests/driver/test_lambda_return_inference_boundary.py` so the existing
   annotated-binding cases are described as contextual typing and stored source
   is described as `HCall(fn=HVar)` with an indirect CallInfo target.

Slawomir explicitly approved item 5's comment-only edits.  That approval does
not cover changing existing test source, assertions, diagnostics, or expected
behavior; use a new approval gate if implementation requires any of those.

Keep the compiler at the pending, unreleased 0.35.0 and ABI 22.  Update both
implementer-owned `PROGRESS.md` files and publish one or two uniquely named
`IMPL-PENDING-*` tokens when the combined slice is ready for static review.
