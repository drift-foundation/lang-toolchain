# Planning refresh: true statement-position throwing IIFE

The 2026-08-03 finding remains open and narrowly scoped. Current-tree static
review confirms the deliberate lowering boundary is still live, while the
existing throwing-IIFE test is still value-position despite its name.

The refreshed recommendation is test-only if the work repro remains exit 0:

1. accurately rename/reword the existing value-position test without changing
   its source or oracle;
2. add one true statement-position compile/link/run test using the work repro;
3. avoid an all-terminal variant and structural parser test because those add
   separate authorities without improving the runtime route oracle;
4. run focused tests only and return the result for reviewer inspection;
5. leave the final full suite and certification to Slawomir.

This is decision support, not an authoritative implementation recipe. K should
re-run the repro and may correct the proposed boundary if current evidence
disproves it. A red repro converts the task from coverage debt to a
`LANGUAGE_BUG`; no compiler patch is pre-approved by this test-only plan.

The exact existing-test ledger is recorded in `PLAN.md` and still requires
Slawomir's approval before implementation.
