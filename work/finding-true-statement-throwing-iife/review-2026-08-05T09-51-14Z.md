# Planning response review and test-edit approval

K's static planning response is accepted. It independently confirms the live
lowering route, the misclassified existing test, the completeness of the
one-file edit ledger, and the absence of interaction with the causal-Unknown
and pending-lambda changes now under full-suite verification.

Two refinements are adopted:

- the new compile/run pin also excludes the historical `No MIR lowering`
  traceback signature;
- `test_stored_capturing_lambda_diagnostic.py` joins the focused gates because
  the literal IIFE body is throw-only with an explicit result type.

The explicit `return 99` remains mandatory to isolate this route from the
separate divergent-finalization authority.

Slawomir approved the exact existing-test ledger in `PLAN.md` on 2026-08-05:
only `lang/tests/driver/test_stmt_position_iife.py` may change, limited to the
accurate rename/comment correction and the new true statement-position
compile/run test. No other existing test edit is authorized.

Planning is complete. Implementation must not start until the preceding
`run_all_tests.sh` gate completes cleanly and the reviewer sends a fresh Baton
start handoff. K remains free to stop and report contrary runtime evidence when
the work repro is re-run; a red repro triggers the documented LANGUAGE_BUG
fork rather than an expectation change.
