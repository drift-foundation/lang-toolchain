# Review pass: Gate 1 RED evidence

## Scope

Review of implementer Baton message `c40e4fdf2dff86cbbfbe253f2d500793` (`red_regression_evidence`, outcome `red_confirmed_awaiting_gate3`) and the corresponding test-only diff. This is not final implementation sign-off.

## Independent checks

**Confirmed:** the current diff at this pass changes only `lang/tests/driver/test_abi_version_stamp.py`. No producer, reader, version, ABI, stdlib, application, or specification file is changed.

**Confirmed:** the new `test_build_info_survives_asan_link` is a linked-binary companion to the normal-link regression. It uses the real ASAN runtime archive, passes `-fsanitize=address -g`, and reads the final executable through production `extract_build_info`. It is not a synthetic padded-payload test.

**Observed independently:** reviewer command:

```text
PYTHONPATH=. ./.venv/bin/python -m pytest lang/tests/driver/test_abi_version_stamp.py::test_build_info_survives_asan_link -x -q
```

Result: RED, `1 failed in 22.55s`. `extract_build_info` raises `BuildInfoError: build-info payload is not valid JSON: Extra data: line 1 column 268 (char 267)` at `lang/build_info.py:345`. This independently confirms the required regression-first baseline on the current tree.

**Confirmed by current-tree inspection:** the reader contract is exact canonical bytes and already rejects trailing content; the test fails at that production boundary. The failure does not justify a reader change.

**Corroborated, not independently reconstructed in this pass:** the implementer's IR/object inspection pins the first mutation to ASAN's IR global transform before linking: 267 contract bytes become a 352-byte, align-32 custom section with an 85-byte redzone. The reported sizes match the independently observed JSON boundary and the downstream PushCoin shape. Final review must inspect the producer patch and its post-fix object/section evidence.

## Decision

No blocking finding at Gate 1. Gate 3 producer work may proceed under the existing Slawomir authorization.

The expected patch remains narrowly constrained:

- exempt only `@__drift_build_info` from AddressSanitizer global instrumentation using a supported LLVM representation;
- retain the global in `@llvm.used` and retain the exact custom section, byte length, and alignment contract;
- leave `lang/build_info.py` unchanged;
- cover ASAN and ASAN+UBSAN without changing standalone UBSAN behavior;
- prove the rest of the module remains ASAN-instrumented;
- keep ABI 22 if no compiler/runtime boundary shape changes;
- apply the required compiler minor bump before staging.

The repository documentation names clang 20 as the recommended toolchain and this host has clang 20.1.8. The implementer should validate the final textual IR with the repository's supported/deployed compiler contract and record concrete evidence; do not invent or claim an unverified broader version matrix.

## Final-review gates still open

- GREEN result for the new linked-ASAN regression.
- Normal link and build-info contract/negative regressions remain green.
- Source diff proves no reader tolerance, compatibility path, or workaround.
- ASAN instrumentation remains active elsewhere in the module.
- Final version/ABI decision matches the actual diff.
- Focused verification evidence and full-suite readiness are reported; agents do not run `run_all_tests.sh`.
