# Commit and staged-artifact verification

## Commit identity

- Repository HEAD is `0a86af72b014064a863c10caa9d72f19cb9822ac`.
- Subject: `codegen: exempt the build-info stamp from AddressSanitizer global padding`.
- The committed message exactly matches the reviewed draft and preserves the explicit warning that the top-level `DRIFT_ASAN=1 just test` verdict is unresolved.

## Staged artifact identity

Reviewer independently confirms the staged toolchain's manifest, version JSON, and producer source identify:

- `driftc 0.36.0`;
- runtime ABI 22;
- source stamp `0a86af72`;
- `no_sanitize_address` present on the build-info global emitter.

K additionally reports a staged-toolchain end-to-end normal/ASAN build and inspect result: both documents accepted; profiles `optimized` and `asan`; ASAN `.drift_build_info` size 295, align 1, zero trailing NUL bytes. Reviewer accepts this as corroborating staged-artifact evidence while downstream teams perform independent pre-cert trials.

## Disposition

- Commit/staged candidate identity: **CONFIRMED**.
- Exact bug contract on K's staged smoke: **GREEN**.
- PushCoin/Web independent pre-cert trials: **PENDING**.
- Top-level ASAN suite criterion 6: **UNRESOLVED** pending maintainer console evidence.
- Certification and downstream third notice: **HELD**.

## Baton references

- K staged-artifact report: `2dedc6b4953aff5aa44aa3dc5005f8ef`
- Reviewer response: `a0c8e22f1b970adaed880e0efc6f28b4`

No source, test, implementer-owned `PROGRESS.md`, or Git mutation is made by this review.
