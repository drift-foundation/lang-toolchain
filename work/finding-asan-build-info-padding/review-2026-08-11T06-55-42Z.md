# Review pass: downstream regression-coverage correction

## Incoming finding

PushCoin review `eaaed695bbc9287cd790feff1e554f73` independently accepted the root fix but found that the promised linked-ASAN regression does not pin the ASAN build profile. Their independent run passed, yet the extracted document reported `"build":{"profile":"optimized"}`.

## Independent confirmation

**Confirmed:** `test_build_info_survives_asan_link` calls `_compile_simple_program` before invoking `clang -fsanitize=address`. The helper delegates to `compile_to_llvm_ir_for_tests`, which resolves the stamped profile from `DRIFT_ASAN` / `DRIFT_UBSAN` / `DRIFT_DEBUG` at IR generation time. The test does not set `DRIFT_ASAN`, so it emits an `optimized` stamp and only sanitizes at the later link/transform step.

**Confirmed:** the test asserts only compiler version and ABI. It does not assert `doc["build"]["profile"] == "asan"`.

**Conclusion:** the byte-contract regression is real and the producer fix remains valid, but the finding's stated acceptance criterion and downstream instrumentation gate are not fully pinned. This is a required correction within the current finding, not a separate queued finding.

## Changes required

1. Make `test_build_info_survives_asan_link` compile the IR with the ASAN profile signal active before `_compile_simple_program` runs (the expected minimal shape is the pytest `monkeypatch` fixture plus `monkeypatch.setenv("DRIFT_ASAN", "1")`).
2. Keep the real ASAN runtime archive and `-fsanitize=address -g` link path.
3. Assert `doc["build"]["profile"] == "asan"` in addition to compiler version and ABI.
4. Re-run the corrected test and both complete build-info test files; report exact results.
5. Update `PROGRESS.md` with the downstream review, independent confirmation, correction, and results.
6. Send `pushcoin.reviewer` a second durable notice acknowledging and closing this coverage discrepancy while retaining the full-suite/release hold.
7. Complete the already-requested permanent 0.36.0 `doc/history.md` entry.
8. Return a combined handoff for final review. Do not change the producer fix or reader, stage/commit, or run `run_all_tests.sh`.

The durable PushCoin review is materialized in this folder as `downstream-review-2026-08-11T06-54-39Z-eaaed695bbc9287cd790feff1e554f73.md`.
