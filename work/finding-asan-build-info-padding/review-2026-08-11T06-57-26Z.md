# Review pass: PushCoin byte/character evidence correction

PushCoin Baton message `d902e39734aa14caae971af4fbb48c53` corrects the downstream forensic measurement:

- ELF section: 896 bytes, align 32;
- canonical JSON: 706 UTF-8 bytes;
- decoded JSON: 704 characters;
- ASAN padding: 190 NUL bytes;
- embedded profile: `asan`.

The original report and earlier upstream review prose conflated Python's `JSONDecodeError` character offset with a raw ELF byte offset. One em dash in the payload is one decoded character encoded as three UTF-8 bytes, accounting for the two-byte difference.

This correction supersedes every earlier statement in this finding that the PushCoin payload was 704 bytes with 192 NULs, including arithmetic repeated in materialized immutable Baton responses and prior append-only reviews. Those historical artifacts are not edited; this review is the current authority for the downstream measurement.

The correction does not change:

- the 896-byte / align-32 section observation;
- the LANGUAGE_BUG classification;
- the upstream ASCII fixture's independent 267-byte to 352-byte / 85-NUL reproduction;
- the compile-time ASAN global-redzone root cause;
- the narrow `no_sanitize_address` producer fix;
- the fail-closed reader contract;
- the reopened requirement that the automated regression compile under `DRIFT_ASAN=1` and assert profile `asan`.

Required before sign-off: implementer-owned `PROGRESS.md`, the cross-team release announcement, and the second PushCoin notice must carry the corrected byte/padding evidence. Permanent `doc/history.md` does not state the incorrect downstream numbers and needs no evidence correction.
