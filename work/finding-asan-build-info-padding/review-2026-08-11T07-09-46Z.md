# Final review: signed off for maintainer full suite

## Findings

No open code, test, version, ABI, history, evidence, or handoff finding remains in the reviewed slice.

## Reviewed diff

- `doc/history.md`: permanent standalone 0.36.0 LANGUAGE_BUG entry.
- `lang/codegen/llvm/llvm_codegen.py`: `no_sanitize_address` on the internal build-info metadata global; exact section bytes/alignment/name and `@llvm.used` retention preserved.
- `lang/tests/driver/test_abi_version_stamp.py`: linked-ASAN regression, compiled with `DRIFT_ASAN=1`, linked through the real ASAN path, read through production `extract_build_info`, and asserting compiler version, ABI 22, and `build.profile == "asan"`.
- `lang/versions.py`: user-visible pre-1.0 minor bump from 0.35.0 to 0.36.0; ABI remains 22.

`lang/build_info.py`, runtime, stdlib, application code, and language specification are unchanged. There is no reader tolerance, NUL trimming, compatibility path, workaround, or compiler/runtime ABI shape change. `git diff --check` passes.

## Regression-first and independent verification

The reviewer independently observed the linked-ASAN regression RED before the producer change:

```text
1 failed in 22.55s
BuildInfoError: build-info payload is not valid JSON:
Extra data: line 1 column 268 (char 267)
```

On the strengthened final test diff, before the one-line environment-lifetime cleanup, the reviewer ran both build-info files whole:

```text
PYTHONPATH=. ./.venv/bin/python -m pytest -n16 lang/tests/codegen/test_build_info_stamp.py lang/tests/driver/test_abi_version_stamp.py -q
63 passed in 87.14s
```

After the final cleanup (keeping `DRIFT_ASAN=1` active through the whole test so profile resolution and `sanitizer_timeout` see one consistent lane), the reviewer reran the directly affected regression:

```text
PYTHONPATH=. ./.venv/bin/python -m pytest lang/tests/driver/test_abi_version_stamp.py::test_build_info_survives_asan_link -q
1 passed in 19.09s
```

The implementer also demonstrated the strengthened test remains RED with the producer attribute temporarily absent and restored the producer file before handoff. No agent ran `run_all_tests.sh`.

## Evidence corrections and downstream state

The authoritative PushCoin measurement is 706 canonical UTF-8 bytes / 704 decoded characters plus 190 NUL bytes in an 896-byte, align-32 ASAN section. This supersedes the original 704-byte / 192-NUL statement, which confused the JSON decoder's character offset with the ELF byte offset. The upstream ASCII fixture's independent 267-byte to 352-byte / 85-NUL reproduction is unchanged.

Live finding/repro artifacts, implementer PROGRESS, and the cross-team announcement carry the correction. Earlier durable messages/reviews remain immutable and are explicitly superseded by the append-only correction review. `doc/history.md` never stated the incorrect downstream numbers.

A second durable notice was sent to `pushcoin.reviewer`, closing the profile-coverage discrepancy and carrying the corrected byte evidence while retaining the full-suite/release hold. PushCoin has not yet consumed that queued delivery; this does not authorize downstream adoption.

## Sign-off state

**SIGNED OFF for Slawomir's full repository suite.** This is not release/certification completion:

- `run_all_tests.sh` remains to be run by Slawomir.
- Nothing is staged or committed.
- PushCoin remains held until the full-suite and release/certification gate pass.
- The finding folder remains until merge/closure and must then be removed with no permanent references to it.
