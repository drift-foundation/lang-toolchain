# Review pass: Gate 5 closure

## Verified

- The release announcement exists at `/tmp/drift-announce/2026-08-11T06-50-31Z-drift-lang-release-notes.md` and accurately records 0.36.0, ABI 22 unchanged, root cause, narrow fix, RED-to-GREEN regression, focused verification, downstream status, and full-suite ownership.
- The downstream Baton notice `ce31849ddffc3f7369b94387d75fe375` was sent to `pushcoin.reviewer` with outcome `fixed_pending_full_suite_and_release_gate`; PushCoin has claimed it.
- Implementer-owned `PROGRESS.md` records the Gate 5 identifiers and remains explicit that the full suite has not run.
- Gate 5 introduced no source/test diff. The implementation diff remains the three previously reviewed files, and `git diff --check` passes.

## Review state

**One bookkeeping change requested before sign-off:** add the permanent 0.36.0 entry at the top of `doc/history.md`.

Rationale: `doc/history.md` is this repository's durable compiler-release ledger; the current top entry is 0.35.0 and every version bump is recorded there. The `/tmp/drift-announce` note is the required cross-team announcement but does not replace the in-repository release history. Shipping `DRIFTC_VERSION = 0.36.0` without a 0.36.0 history entry would leave the permanent tree contradictory.

The entry should stand alone after the ephemeral finding is removed and include:

- 0.36.0 title/date and ABI 22 unchanged;
- the ASAN `.drift_build_info` exact-byte failure and downstream impact;
- the compile-time ASAN global-redzone root cause;
- the producer-only `no_sanitize_address` fix with the fail-closed reader unchanged;
- the linked-ASAN RED-to-GREEN regression and focused verification;
- version/ABI rationale and no downstream source migration/workaround;
- full-suite status stated accurately until Slawomir runs it.

Do not reference the finding folder, Baton ids, or `/tmp` announcement from the permanent history entry. No further compiler, reader, runtime, stdlib, or test change is requested. Update `PROGRESS.md`, return the history-only diff for review, and do not stage, commit, or run `run_all_tests.sh`.
