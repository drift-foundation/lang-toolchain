# Commit-message and staged-index review

## Commit-message text

K's proposed commit-message text is approved for the intended four tracked implementation/history files. It accurately covers the codegen root fix, fail-closed reader preservation, linked-ASAN regression, 0.36.0 minor bump, unchanged ABI 22, history entry, focused verification, and currently unresolved top-level ASAN verdict. If the maintainer supplies a terminal verdict before commit, the final unresolved-verdict paragraph must be replaced with the verified result.

The approved draft is `COMMIT_MSG.draft.txt`. This is a temporary finding artifact, not permanent source material.

## Staged-index findings

Read-only review found the current index is not commit-ready:

- 29 staged paths, 2,141 insertions and 2 deletions;
- the whole ephemeral finding folder is staged in addition to the four implementation/history files;
- `PROGRESS.md` is `AM`, so staged and working snapshots differ;
- `git diff --cached --check` reports trailing whitespace in `evidence-2026-08-11T15-19Z-overloaded-run.md` lines 13–14;
- the commit-message draft itself is untracked.

No reviewer Git mutation is authorized or performed. K may correct the two working-file whitespace defects and keep implementer-owned `PROGRESS.md` current, but only Slawomir may decide and mutate final staged scope. The finding-folder removal/no-residual-reference rule remains binding after merge and closure.

## Disposition

- Commit text: **APPROVED FOR INTENDED FOUR-FILE CHANGE, WITH LIVE VERDICT PARAGRAPH**.
- Current index: **NOT APPROVED / NOT COMMIT-READY**.

## Baton references

- K draft: `f6bcb65687e70bd33f8cd32d6f432dc7`
- Reviewer response to K: `9760b84a3a0641f1c8eb6215b9484722`
- Reviewed text and index report to Slawomir: `fcf2a3bd852c7cbfbd5500c54fefe27a`
