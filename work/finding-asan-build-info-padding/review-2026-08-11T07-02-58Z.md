# Review pass: strengthened ASAN-profile regression

## Verified

- The amended test sets `DRIFT_ASAN=1` before `_compile_simple_program`, so `compile_to_llvm_ir_for_tests` resolves and stamps the `asan` profile during codegen.
- The test retains the real ASAN runtime archive, `-fsanitize=address -g` transform/link path, and production `extract_build_info` reader.
- It now asserts `doc["build"]["profile"] == "asan"` alongside compiler version and ABI, closing PushCoin's coverage finding without narrowing the original acceptance criterion.
- The producer fix, reader, version, ABI, runtime, and stdlib are unchanged by this amendment.
- `git diff --check` passes.

Independent reviewer run on the amended diff:

```text
PYTHONPATH=. ./.venv/bin/python -m pytest -n16 lang/tests/codegen/test_build_info_stamp.py lang/tests/driver/test_abi_version_stamp.py -q
```

Result: `63 passed in 87.14s`.

## One test-hygiene change requested

Remove the explicit `monkeypatch.delenv("DRIFT_ASAN", raising=False)` immediately after IR generation. The pytest fixture already restores the prior environment at test teardown. Deleting the lane signal mid-test makes the claimed "whole ASAN lane" internally inconsistent and causes the later `sanitizer_timeout(120)` call to miss its documented ASAN multiplier. It also temporarily erases an outer ASAN-lane value when the suite itself started with `DRIFT_ASAN=1`.

Keep `DRIFT_ASAN=1` active for the whole test body and rely on `monkeypatch` teardown for scoped restoration. Then rerun the corrected single test and report the result. The complete 63-test suite does not need another run for this one-line environment-lifetime cleanup unless another substantive diff appears.

Overall sign-off also remains pending the already-relayed 706-byte / 190-NUL evidence correction in `PROGRESS.md` and the cross-team announcement, plus the second PushCoin notice. No producer or reader change is requested.
