# Review pass: implementation and focused verification

## Scope

Review of implementer handoff `d62570697295dcb4ca9070ce7b8ddb35` (`implementation_handoff`, outcome `gate4_green_awaiting_final_review`), the complete source/test/version diff, and focused verification. This supersedes no earlier review journal; reviews are append-only.

## Code findings

No blocking code finding.

The patch is narrowly scoped and conforms to the finding contract:

- `LlvmModuleBuilder.emit_build_info` adds only the `no_sanitize_address` LLVM global attribute to `@__drift_build_info`.
- The section name, exact array length, `align 1`, and `@llvm.used` retention remain intact.
- `lang/build_info.py` is untouched; there is no NUL trimming, tolerance, compatibility path, or second reader contract.
- The authorized linked-ASAN regression exercises the real ASAN archive/link path and production `extract_build_info`.
- `DRIFTC_VERSION` moves from `0.35.0` to `0.36.0`, correctly treating the user-visible fix as a pre-1.0 minor release.
- `DRIFT_RT_ABI_VERSION` remains 22; the diff changes no exported runtime signature, cross-boundary layout, calling convention, ownership contract, or other compiler/runtime ABI shape.
- `git diff --check` passes.

The implementation evidence also shows the exemption is narrow: the build-info ASAN descriptor disappears while thousands of other globals and the ASAN module constructor remain instrumented. Standalone UBSAN remains unaffected; ASAN and ASAN+UBSAN preserve the exact section.

## Independent verification

Reviewer command on the final diff:

```text
PYTHONPATH=. ./.venv/bin/python -m pytest -n16 lang/tests/codegen/test_build_info_stamp.py lang/tests/driver/test_abi_version_stamp.py -q
```

Result: `63 passed in 82.60s`. This includes the new linked-ASAN regression, the normal-link companion, schema/canonicalization coverage, and malformed/duplicate/trailing-content negatives.

The earlier review pass independently observed the new regression RED before the producer change: `1 failed in 22.55s`, `Extra data: line 1 column 268 (char 267)`. Regression-first ordering is therefore pinned independently, not accepted from the status summary alone.

## Review state

**Changes requested — Gate 5 only.** The code/test/version slice is approved, but the finding is not signed off until the required cross-team closure artifacts exist:

1. Publish `/tmp/drift-announce/<iso-utc>-drift-lang-release-notes.md` describing the 0.36.0 ASAN build-info fix, ABI 22 unchanged, RED-to-GREEN regression, focused results, and readiness for Slawomir's full-suite run.
2. Send `pushcoin.reviewer` a durable Baton handoff with the pinned regression, root-cause/fix evidence, version/ABI result, and explicit statement that its blocked ASAN `drift inspect build-info ... --json` gate may resume after the tracked/full-suite release gate.
3. Update implementer-owned `PROGRESS.md` with those publication/message identifiers and the final state awaiting Slawomir's full suite.
4. Return a Gate 5 handoff to `lang.reviewer`; do not stage, commit, or run `run_all_tests.sh`.

No additional source or test edit is requested. If Gate 5 introduces a source-tree diff, disclose and justify it for a fresh code review.
