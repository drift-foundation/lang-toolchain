import std.console.out;

error A {}
error B {}
fn fire() -> Void {
  throw B()
}

fn main() -> Int {
  try {
    fire()
  } catch A(e) {
    out.writeln("A")
  } catch err {
    out.writeln("fallback")
  }
  return 0
}
