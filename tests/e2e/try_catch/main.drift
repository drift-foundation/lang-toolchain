import std.console.out

fn might_fail(x: Int) returns Int {
  if x != 0 {
    throw drift_error_new_dummy(x, "payload", "")
  }
  return x
}

fn main() returns Int {
  try {
    out.writeln("before")
    might_fail(1)
  } catch err {
    out.writeln("caught")
  }
  return 0
}
