exception MyError(a: String)

fn make_error() returns Void {
  throw MyError(a = "AA")
}

fn main() returns Int64 {
  var code: Int64 = 0
  try {
    make_error()
  } catch Error(e) {
    code = e.code
  }
  return code
}
