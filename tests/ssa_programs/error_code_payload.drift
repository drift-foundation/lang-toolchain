fn make_error() returns Error {
  // code=7, payload="p".
  return drift_error_new_dummy(7, "payload", "p")
}

fn main() returns Int64 {
  val err = make_error()
  // Access Error.code and Error.payload; return code plus payload length literal.
  val code: Int64 = err.code
  // payload is a String; load it but don't call methods (SSA scaffold).
  val payload = err.payload
  return code
}
