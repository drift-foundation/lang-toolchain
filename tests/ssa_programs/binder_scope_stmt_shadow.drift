exception MyError(msg: String)

fn may_fail() returns Int {
  throw drift_error_new_dummy(1, "msg", "")
}

fn main() returns Int {
  var e: Int = 5
  try {
    may_fail()
  } catch err {
  }
  return e
}
