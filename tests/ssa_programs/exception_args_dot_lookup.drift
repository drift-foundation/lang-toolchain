exception MyError(a: String, b: String)

fn make_error() returns Error {
  throw MyError(a = "AA", b = "BBB")
}

fn main() returns Int {
  try {
    make_error()
  } catch MyError(e) {
    val view = e.args
    val _a = view[view.a()]
    val _b = view[view.b()]
    return 0
  }
  return 0
}
