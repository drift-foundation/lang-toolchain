module main;

import std.console as console;

error E { n: Int, s: String, b: Bool }
fn fail() -> Int {
	throw E(n = 1, s = "x", b = false);
}

fn main() nothrow -> Int{
    try fail() catch err {
        console.print("ok");
    }
    return 0;
}
