module main;

import std.console as console;

error E { msg: String }
fn fail() -> Int {
	throw E(msg = "boom");
}

fn main() nothrow -> Int{
    try fail() catch err {
        console.print("caught");
    }
    return 0;
}
