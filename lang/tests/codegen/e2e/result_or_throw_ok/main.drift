module m;

import std.core as core;

pub error MyErr {
	code: Int,
}

pub fn main() nothrow -> Int {
	val r: core.Result<Int, MyErr> = core.Result::Ok(7);
	val v = try (move r).or_throw() catch MyErr(_) { 1 } catch { 2 };
	return v;
}
