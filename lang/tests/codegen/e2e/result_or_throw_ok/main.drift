module m;

import std.core as core;
import std.err as err;

pub fn main() nothrow -> Int {
	val r: core.Result<Int, Int> = core.Result::Ok(7);
	val v = try (move r).or_throw() catch err:ResultError(_) { 1 } catch { 2 };
	return v;
}
