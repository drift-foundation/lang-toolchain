{
  "description": "maria-v1 Issue 1 e2e: method-receiver autoborrow through a ref-returning rvalue base + projection. Pins the customer-critical shape `w.get().handle.peek()`, a bare-function variant `get_inner_ref(inner).handle.peek()`, the lowering-equivalent baseline `val r = w.get(); r.handle.peek()`, AND the receiver-parity forms closed with reject-redundant-call-borrows: HIndex through a ref-returning base (`w.handles_ref()[1].peek()`), deref-at-base (`(*w.inner_ref()).handle.peek()`), and HIndex through an OWNED rvalue base (`mk_handles()[0].peek()`, materialized into a drop-registered temp by the MIR lifted chain). All six shapes must compile AND produce the expected values at runtime. Sibling to `borrow_chained_ref_projection_noncopy`, which pins the explicit-borrow BINDING forms (SECTION A) and the bare ARGUMENT forms (SECTION B) of the same chains.",
  "exit_code": 0,
  "stdout": "ok\n"
}
