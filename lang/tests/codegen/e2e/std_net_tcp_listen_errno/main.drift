module m;

import std.net as net;
import std.concurrent as conc;

pub fn main() nothrow -> Int {
	var addr = net.socket_addr("127.0.0.1", 0);
	val t = conc.Duration(millis = 1000);
	var lr = net.listen(&addr, t);
	match lr {
		Ok(_l) => { return 0; },
		Err(e) => {
			val code = match e {
				Errno(c) => { c },
				WouldBlock => { 201 },
				default => { 202 }
			};
			return code;
		},
		default => { return 203; }
	}
}
