module main;

import std.env as env;

fn main() nothrow -> Int {
	// A variable that should never be set.
	match env.get("DRIFT_E2E_TEST_UNSET_VAR_XYZ_99") {
		Optional::Some(_v) => {
			return 1;
		},
		Optional::None() => {
			return 0;
		}
	}
}
