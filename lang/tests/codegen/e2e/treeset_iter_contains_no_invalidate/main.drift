module main

import std.containers as containers;
import std.iter as iter;
use trait iter.SinglePassIterator;

fn run() -> Int {
	var s = containers.tree_set<type Int>();
	s.insert(1);
	s.insert(2);
	s.insert(3);
	var it = s.iter();
	val k1 = 1;
	val _g = s.contains(&k1);
	var count = 0;
	var done = false;
	while not done {
		val nxt = it.next();
		match nxt {
			None => { done = true; },
			Some(_) => {
				count = count + 1;
			}
		}
	}
	if count != 3 { return 1; }
	return 0;
}

fn main() nothrow -> Int {
	return try run() catch { 99 };
}
