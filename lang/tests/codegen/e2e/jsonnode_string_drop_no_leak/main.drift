module m

import std.core as core;
import std.json as json;

fn run() -> Int {
	var i = 0;
	while i < 2000 {
		val n = json.JsonNode::String("abc");
		core.drop_value<type json.JsonNode>(move n);
		i = i + 1;
	}
	return 0;
}

fn main() nothrow -> Int {
	return try run() catch { 99 };
}
