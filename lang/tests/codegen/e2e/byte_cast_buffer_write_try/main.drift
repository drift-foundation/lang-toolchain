module m;

import std.io as io;
import std.core as core;
use trait core.Try;

fn write_byte() throws -> Int {
	var buf = io.buffer(1);
	val b = cast<Byte>(65);
	io.buffer_write(&mut buf, 0, b);
	return 0;
}

pub fn main() nothrow -> Int {
	return try write_byte() catch { 1 };
}
