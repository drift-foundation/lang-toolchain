module m;

import std.sync as sync;

pub fn main() nothrow -> Int {
	var a = sync.atomic_bool(false);
	a.store(true, sync.MemoryOrder::Relaxed());
	val v = a.load(sync.MemoryOrder::Relaxed());
	if v {
		return 0;
	}
	return 1;
}
