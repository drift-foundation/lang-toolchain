module m;

import std.core as core;

pub fn main() nothrow -> Int{
    val s0 = "ążć";
    val s1 = "żół";
    val n = s0.byte_length();
    val expected = s1.byte_length();  // both are 6-byte UTF-8 strings
    if n == expected {
        return 0;
    }
    return 1;
}
