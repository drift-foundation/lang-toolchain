module main

import std.console as console;

exception A()
exception B()

fn fail() -> Int {
	throw A();
}

fn main() nothrow -> Int{
    try fail() catch A(e) {
        console.print("a");
    } catch {
        console.print("all");
    }
    return 0;
}
