module main;

import std.console as console;

error A {}
error B {}
fn fail() -> Int {
	throw A();
}

pub fn main() nothrow -> Int{
    try fail() catch A(e) {
        console.print("a");
    } catch {
        console.print("all");
    }
    return 0;
}
