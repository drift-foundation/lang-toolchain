module main;

import std.concurrent as conc;

pub fn main() nothrow -> Int {
	var x: Int = 42;
	try {
		conc.scope(|_s: std.concurrent.Scope| captures(copy x) nothrow => { val y = x; return; });
		return 0;
	} catch e { return 2; }
}
