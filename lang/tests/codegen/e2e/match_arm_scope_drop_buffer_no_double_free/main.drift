module m;

import std.core as core;
import std.io as io;

pub fn main() nothrow -> Int {
	val r: core.Result<Int, Error> = core.Result::Ok(1);
	match r {
		Ok(v) => {
			var buf = io.buffer(1);
			io.buffer_write(buf, 0, cast<Byte>(65));
			if v == 2 {
				return 1;
			}
		},
		default => {}
	}
	return 0;
}
