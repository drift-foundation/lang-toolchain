module m;

import std.log as log;

fn run() -> Int {
	var b = log.config_builder();
	b.min_level(log.Level::Error());
	val lg = log.create_logger("main", b.build());
	if lg.info("info", {"x": 1}) {
		return 2;
	}
	if not lg.error("err", {"x": 2}) {
		return 3;
	}
	return 0;
}

fn main() nothrow -> Int {
	return try run() catch { 99 };
}
