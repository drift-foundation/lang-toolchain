{"description": "reject: bitcopy field (.count: Int) projected from a droppable-root ternary (PR owns String). The DOUBLE-registered owner is the root PR, not the Int leaf, so a leaf-bitcopy guard would wrongly accept and double-free the root's String. Rejected bind-first via the root has_drop check.", "exit_code": 1, "diagnostics": [{"message_contains": "temporary produced by a conditional (ternary or match) expression; bind it to a `var` first", "phase": "typecheck"}]}
