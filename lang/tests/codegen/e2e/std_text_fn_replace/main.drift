module main;

import std.core as core;
import std.text as text;

fn str_eq(a: &String, b: &String) nothrow -> Bool {
	val an = a.byte_length();
	val bn = b.byte_length();
	if an != bn { return false; }
	var i = 0;
	while i < an {
		if core.string_byte_at(a, i) != core.string_byte_at(b, i) { return false; }
		i = i + 1;
	}
	return true;
}


pub fn main() nothrow -> Int {
	val r = text.replace("aaa", "a", "bb");
	val e = "bbbbbb";
	if !str_eq(r, e) { return 1; }
	// Not found → unchanged
	val r2 = text.replace("hello", "xyz", "a");
	val e2 = "hello";
	if !str_eq(r2, e2) { return 2; }
	// Empty old → unchanged
	val r3 = text.replace("hello", "", "x");
	val e3 = "hello";
	if !str_eq(r3, e3) { return 3; }
	// Replace with empty (deletion)
	val r4 = text.replace("a-b-c", "-", "");
	val e4 = "abc";
	if !str_eq(r4, e4) { return 4; }

	return 0;
}
