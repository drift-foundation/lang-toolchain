module main;

import std.console as console;

error E { code: Int, fatal: Bool, msg: String }
fn fail() -> Int {
	throw E(code = 123, fatal = true, msg = "hello");
}

pub fn main() nothrow -> Int{
    try fail() catch err {
        console.print("caught");
    }
    return 0;
}
