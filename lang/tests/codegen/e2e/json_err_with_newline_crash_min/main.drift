module m;
import std.core as core;
import std.json as json;

fn run() -> Int {
  val t = "{\n\"a\":}\n";
  match json.parse(&t) {
    core.Result::Ok(_) => { return 1; },
    core.Result::Err(_e) => { return 0; }
  }
}

fn main() nothrow -> Int { return try run() catch { 99 }; }
