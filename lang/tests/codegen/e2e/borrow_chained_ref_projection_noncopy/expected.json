{
  "description": "Chained projection borrows over rvalue bases, non-Copy leaves — DUAL-route pin. SECTION A: source-written borrows in NON-ARGUMENT bindings (`val r = &w.get().handle` / `&w.handles_ref()[i]` / `&*w.inner_ref()`), the original 2026-05-14 stage1 `_split_lift_place_chain` regression (mariadb team): HField/HIndex/HUnary(DEREF) arms, exits 11-15. SECTION B: the SAME chains BARE in argument position at a declared `&T` formal (reject-redundant-call-borrows spellings) — the typed checker synthesizes the HBorrow after stage1, so lowering goes through the MIR twin `_lift_rvalue_ref_base_for_borrow`/`_validate_lifted_chain` (field, index, deref-at-base hops), exits 1-5. SECTION C: OWNED rvalue base + index bare (`_peek(mk()[1])`, materialized into a drop-registered temp), exit 21. SECTION D: NESTED index hops bare (`_peek(mk_matrix()[0][0])` — the shallow checker suppresses the element-copy check for EVERY HIndex on the borrow-subject spine, round-5 fix), exits 22-23. All sections must compile AND produce the expected values at runtime. Receiver-position siblings: `autoborrow_method_receiver_through_ref_rvalue_chain`.",
  "exit_code": 0,
  "stdout": "ok\n"
}
