module m;

import std.concurrent as conc;

pub fn main() nothrow -> Int {
	val x = 7;
	var t = conc.spawn(| | captures(copy x) => { return x; });
	val r = t.join();
	match r {
		Ok(v) => { return v; },
		Err(_) => { return 1; },
		default => { return 2; }
	}
}
