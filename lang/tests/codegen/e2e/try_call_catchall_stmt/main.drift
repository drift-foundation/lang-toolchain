module main;

import std.console as console;

exception Boom()

fn foo() -> Int {
	throw Boom();
}

fn bar() -> Int {
	throw Boom();
}

fn main() nothrow -> Int{
	try {
		foo();
		bar();
	} catch {
		console.print("caught");
	}
	return 0;
}
