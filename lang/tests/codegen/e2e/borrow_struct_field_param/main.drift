module main;

struct Point(x: Int, y: Int);

fn inc_x(p: &mut Point) nothrow -> Void {
	val rx = &mut (*p).x;
	*rx = *rx + 1;
}

pub fn main() nothrow -> Int{
	var p = Point(41, 0);
	inc_x(p);
	return p.x;
}
