// Scalar match without a default is non-exhaustive (v1 always requires
// default for integer domains).
module m;
pub fn main() nothrow -> Int {
	val n = 1;
	val r = match n {
		0 => { 1 },
		1 => { 2 },
	};
	return r;
}
