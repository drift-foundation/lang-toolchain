module m

import std.concurrent as conc;

pub fn main() nothrow -> Int {
	var a = conc.atomic_int(0);
	val old = a.fetch_add(5, conc.MemoryOrder::Relaxed());
	val now = a.load(conc.MemoryOrder::Relaxed());
	if old == 0 and now == 5 {
		return 0;
	}
	return 1;
}
