module main

import lang.thread as thread;

fn main() nothrow -> Int {
	var buf = unsafe { thread.array_byte_alloc_uninit(4) };
	unsafe { thread.array_byte_commit_init_len(&mut buf, -1); }
	return 0;
}
