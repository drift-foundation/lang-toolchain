module m;

import std.core as core;

pub error MyErr {
	code: Int,
}

pub fn main() nothrow -> Int {
	val r: core.Result<Int, MyErr> = core.Result::Err(MyErr(code = 1));
	val out = try (move r).or_throw() catch MyErr(_) { 7 } catch { 8 };
	return out;
}
