module m;

fn mk() nothrow -> Array<Byte> {
	var out: Array<Byte> = [];
	out.push(cast<Byte>(1));
	return move out;
}

pub fn main() nothrow -> Int {
	val b = mk();
	if b.len != 1 { return 1; }
	return 0;
}
