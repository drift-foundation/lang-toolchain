module m;

import std.console as console;

fn main() nothrow -> Int{
	var xs = ["a", "b"];
	val popped = xs.pop();
	val _ = match popped {
		Some(v) => { console.println(v); 0 },
		None => { console.println("none"); 0 },
	};
	console.println(xs[0]);
	return 0;
}
