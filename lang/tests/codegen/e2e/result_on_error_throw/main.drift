module m;

import std.core as core;

exception MyError(code: Int);

pub fn main() nothrow -> Int {
	val r: core.Result<Int, Int> = core.Result::Err(5);
	val v = try r.on_error(|e: Int| => { throw MyError(e); }) catch MyError(_) { 7 } catch { 8 };
	return v;
}
