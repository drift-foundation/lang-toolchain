module main;

import std.env as env;

fn main() nothrow -> Int {
	// HOME is always set on Linux.
	match env.get("HOME") {
		Optional::Some(v) => {
			// Non-empty value expected.
			if v.byte_length() == 0 {
				return 1;
			}
			return 0;
		},
		Optional::None() => {
			return 2;
		}
	}
}
